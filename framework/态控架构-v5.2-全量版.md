# 态控架构 v5.2（全量版）

**定位**：智能体设计总纲——回答"如何系统化地构建一个计算输出可控和成本最优的智能体"。

**态控架构的目的**：在状态机的调度约束下让所有场景大模型（智能体）和用户适配适合的输入和输出。

**文档版本**：v5.2
**日期**：2026-07-17

> **v5.0 变更记录（相对 v4.7）**
>
> 本次是架构自 v4.0 以来最大的一次结构变更，核心原则：按约束对象分层，每一层的约束对象唯一。
>
> 1. **§7 约束体系重写**——四层变三层：根宪法（8条→4条，约束所有环节模型）+ 环节宪法（强化第一条 off-task）+ 领域规则（L0-L3，不变）+ 架构机制（新增，约束调度器/DET）。公共规则取消（内容分拆到根宪法/环节宪法/架构机制）
> 2. **ANALYZING 重新定义**——ANALYZING 是"意图识别环节"（非"非环节"），与 IN_SESSION 统一受环节隔离约束
> 3. **§3.1 扩展**——新增 off-task 双重保障机制（宪法强制 + ANALYZING 重新识别验证），取消 DET 主动介入。v5.1 修正：保障2 从"DET事后验证"改为"ANALYZING重新识别验证"（DET做不了语义验证）
> 4. **附录 E（新增）**——编程规则宪法（6 条全局元约束），约束代码模型不得以正则/DET 替代 LLM
> 5. **§4.3 N14 审骨架更新**——新增执行体分派验证（静态分析 + 运行时验证）
>
> **v5.1 变更记录（相对 v5.0）**（2026-07-17）
>
> 1. **§3.1.2 ANALYZING 增加第二分类维度**——在意图匹配（A/B/C/D/E）之外，并行分类输入性质：S（闲聊/松弛）/T（任务导向）/U（不确定）。DET 交叉裁决三路路由：已知房间 / SLACK_NODE / TEMP_TOPIC。**一次 ANALYZING 调用，不改模型，不追加 LLM 调用。**
> 2. **§4.2 新增 TEMP_TOPIC 节点**——运行时创建的房间，仅根宪法4条约束，无环节管道。一旦用户命名即纳入房间体系：上下文管理器管理、roomStateIndex索引、"切断房间"切换、可产出。与正式房间和SLACK_NODE形成三级约束梯度。
> 3. **§4.2.1 SLACK_NODE 切入统一体系**——SLACK_NODE 同样用"切断房间"退出回 LISTENING，三者统一 M1 切换指令。
>
> **v5.2 变更记录（相对 v5.1）**（2026-07-17）
>
> 1. **§3.1.4 新增产出物总索引（Output Registry）**——全系统产出物统一登记簿，存储在 contractStore。支持按房间、类型、关键词 DET 确定性检索（0 LLM）。跨房间产出物可发现、可自动复制到目标房间 workspace，无需用户手动操作文件系统。
> 2. **§2.6 房间比喻扩展**——房间不仅有 roomConversationLog 和 roomStateIndex，产出物也登记在册。跨房间引用通过 outputRegistry + DET 文件复制完成，不打破房间隔离原则（conversationLog 不互通）。
> 3. **§3.1.2 ANALYZING 增强**——ANALYZING prompt 末尾追加相关产出物列表，让意图识别时拥有"全系统产出物视野"。

---

## 一、架构背景

### 1.1 模型是一个无状态的有自己权重分布概率计算单元

模型的计算能力是有限的，而且受到训练结果约束，权重分布是不均衡的。无论是模型本身的计算资源，还是模型外部的计算载体，还有消耗的能源都是有限的，但是要计算的内容是趋向膨胀的。所有AI应用本质是消耗直接能源和间接资源的计算行为，每次单位计算资源的消耗是基本固定的，所以当次计算内容对任务的匹配度越高越好。

AI应用追求计算性价比的路径：首先找到当次输入任务最匹配的内容，然后去合理分配这些内容，把跟任务最匹配的内容放在最适合的位置，然后按这个标准去分配计算内容。

### 1.2 任何任务皆为受限计算

在实际应用中，不存在"纯无限制的自由计算"。写诗、写小说等创意任务同样具有方向、主题与格式限制。在态控视角下，写诗与修改家具参数都是受限空间内的收敛过程，区别在于收敛空间的连续性——前者是在连续语义空间中采样满足约束的样本，后者是在离散参数空间中定位唯一正确路径。

### 1.3 认知局部性原理（输入侧）

任何信息处理主体（人、LLM、确定性程序），在 t 时刻处理节点 N 时，最优输入集 = N 在当前目标下的最小充分条件集（contract-in（输入契约）），不是全集 Γ。这一条是认知必然：

A 人：工作记忆 7±2 组块，逼人看全局反而信息过载、分析瘫痪。

B LLM（推理阶段）：Lost in the Middle（中间迷失） 效应使中部信息召回率显著低于头尾；近因偏置使尾部权重远高于前部；长上下文幻觉率随长度单调递增。喂全局是跟注意力机制对着干。

C 确定性程序（DET）：函数纯度要求 f(x) 只依赖 x，不依赖全局变量。


## 二、架构哲学与设计原则

态控架构是一套以"认知局部性"为哲学根、面向 LLM 智能体收敛空间设计的工程框架。态控架构认为智能体 = 数据流网络 + 三类执行体（确定性程序 / 概率性 LLM / 人类选择）。态控架构根本立场："以数据流为中心，LLM 是被调度的资源"——它把上下文剪裁权从概率模型内部彻底剥离，移交给外部确定性状态机。这个状态机不关心每个节点跑的是什么模型、处理什么模态、由谁执行，只定义节点之间的数据格式和路由规则。每个节点被封装为自治单元。模型只需要精准匹配当前智能体场景的需求。同类执行环节可以定向后训练专用深度模型，理论上每个 intent 都可以配备专用小模型，成本从"全量大模型"降为"按需分配"。

### 2.1 数据流定义

"数据从哪里来、到哪里去、经过什么变换"；执行体定义"每个变换节点由谁执行"。每个节点是一个独立的变换函数，接收系统拼接的结构化输出（contract-in（输入契约）），根据当前节点的场景描述进行处理，产生系统需要的结构化数据（contract-out（输出契约））。节点之间不共享内部状态，不依赖全局上下文。每个节点只拥有它收到的输入数据和它产生的输出数据。

### 2.2 长上下文管理原则

本架构通过数据流切段、契约传递、每轮按规则拼接主动管理上下文长度。每个环节的上下文窗口由上下文管理器控制在高效区间——从 本环节roomConversationLog（全量记录）/对话快照/已确定内容/领域规则+其他环节与本环节关联内容（例如基于字段的场景中前置环节的产出物和基于主题场景中的规则网络图）这些内容中按上下文管理器管的拼接机制进行拼接。

### 2.3 受控灵活性原则

宪法主体在设计时写死，但允许在运行时通过预设的 tunable（可调参数） 槽位调整指定参数。调整范围受 range（范围）/enum（枚举） 限定，变更日志可追溯。

### 2.4 环节级自治原则

态控调度器管路由与全量记录，上下文管理器管上下文拼接，模型管环节内交互。三个实体各司其职：态控调度器只在环节边界执行路由和记录；上下文管理器在环节入口从态控调度器调配的全部内容中组装上下文；环节内的多轮追问、字段校验、偏离检测由模型实例在局部宪法约束下自主完成；态控调度器在关键节点执行确定性兜底校验。

**上下文管理器的实现形态**：上下文管理器的实现形态由场景复杂度决定（确定性程序 / LLM / 混合），属于实现层选择，架构层不固定其形态。

### 2.5 最小可识别单元

态控的最小可识别单元不是"三分+契约+并联+tunable（可调参数）"全套，而是——一个态控调度器 + 一个环节的每状态标执行体 + 每状态 contract-in（输入契约）/out（输出契约） + 一部宪法。这四件套齐了，"态控"就成立。

### 2.6 房间比喻——态控的核心灵魂

态控架构的灵魂是"每个环节就是一个独立子任务房间，态控调度器会判断用户每次想去哪个房间"。

**房间里有什么**：每个环节是一个独立房间，房间里有这个环节上次讨论的全量记录（roomConversationLog——整个环节对话原文）和对话快照（stepSnapshot——collectedFields）+已确认的产出物+相关的领域规则）。门一关，其他房间的内容不进入当前房间的上下文拼接——隔离对象是"其他环节无关内容"，隔离动作是"不进入上下文拼接"，不是"环节之间逻辑上不可见"。门一开，态控调度器把全量记录和对话快照+已确认的产出物+相关的领域规则和用户最新输入一起交给房间内部的上下文管理器，由上下文管理器进行内容拼接，然后输入给模型计算。上下文管理器是独立于环节宪法的专用拼接机制——它只管"怎么从态控调度器交给它的内容中组装出任务计算需要的上下文"。

**模型在这套系统里的真实位置**：模型本身是无状态的。它不知道自己上一轮输出了什么，不知道用户上次改了什么参数。房间里保存的全量记录和对话快照就是模型的上下文——"记性"不来自模型，来自系统的内容保存和分类机制。所以用户可以在房间之间自由穿梭：做完 N10 的 tunable，说一声"改一下 N1 的场景定义"，状态机立刻匹配 intent=N1，取出 N1 房间的相关内容交给 N1 的内容管理器，内容管理器拼接后递给模型。模型看到的只有 N1 房间里的内容，不掺杂 N10 的参数细节。

**自由切换的四个支柱（v5.2 新增产出物索引）**：

| 支柱 | 做什么 |
|------|--------|
| 全量记录（roomConversationLog） | 每个环节的每轮对话实时落盘——切换环节时内容不丢弃，返回时完整可用 |
| 对话快照（stepSnapshot） | 每个环节收敛时的结构化字段快照——快速定位已确认的决策 |
| 规则提炼按环节隔离 | 每个环节的 changeLog 独立推送 ruleMiningQueue——N12 的规则提炼只消费 N12 的变更历史 |
| **产出物总索引（outputRegistry，v5.2 新增）** | **每个房间产出物的统一登记簿——跨房间可发现、可检索、可自动复制。DET 确定性查询（0 LLM）。如"元智能体的L2设计文档"可被专利撰写房间自动发现并复制引用** |

**自由切换的适配目标**：自由切换是用户在真实使用中的实际行为。架构的价值命题是"让切换正确"（正确匹配 intent、取出正确房间内容、不串台），不是"鼓励切换"。

**这就是为什么 field_based 和 topic_based 都需要跨会话记忆**：topic_based 用 topicEvolution 管记录，field_based 用 sessionCheckpoint 管记录——两种机制互不替代，但共享同一个灵魂：模型无状态，状态机管全量记录，内容管理器管拼接，用户自由穿梭。

### 2.7 实现形态声明

本节集中声明各关键组件的实现形态属性，划清架构层固定属性与实现层可变属性的边界，避免下游实现者按默认假设做偏差。

| 组件 | 架构层固定属性 | 实现层可变属性 |
|------|--------------|--------------|
| 态控调度器 | 职责固定——路由 + 全量记录 + 上下文管理器调配；必须是确定性程序 | 路由表结构、记录存储格式、状态转移具体实现 |
| 上下文管理器 | 职责固定——从态控调度器匹配的内容中组装任务计算需要的上下文 | 实现形态可变——确定性程序 / LLM / 混合，由场景复杂度决定；拼接策略、token 上限、降级方案 |
| 环节内执行体 | 职责固定——在局部宪法约束下完成环节内交互 | 实现形态可变——确定性程序 / LLM / 人类选择，由 taskType 和节点状态决定 |
| DET 复验 | 职责固定——确定性收敛，不调 LLM | 校验规则集、值域定义、格式 schema |
| 规则提炼引擎 | 职责固定——从 changeLog 和 topicEvolution 提取候选规则，经用户确认后入库 | 触发条件（时间窗口 / 数量阈值）——但必须保证低频性；提炼算法、交叉验证策略 |
| contractStore | 职责固定——存储契约、规则、演化记录，支持按 topicPath 子图匹配 | 存储格式、索引结构、分层存储策略、GC 策略 |
| taskType 判定 | 判定主体固定——由用户判定 | 判定时机与缓存机制——由实现层根据后续章节定义 |


## 三、核心机制

### 3.1 意图收敛机制

**定位**：从用户模糊的自然语言输入，收敛到系统可执行的结构化指令。

**收敛链路**：
```
用户输入
  → 第0层：口令层（DET EXACT_MATCH，0 次 LLM）
  → 第0.5层：关键词扫描（DET 标记 offTaskSuspicion，0 次 LLM）
  → 第1层：意图识别（LLM 轻量模型，ANALYZING 环节）
  → 第2层：环节内执行（LLM 通用/深度模型，IN_SESSION 环节）
  → 第3层：DET 值域复验（DET 四项检查）
```

#### 3.1.1 off-task 双重保障（v5.0 新增）

**问题**：环节间自由切换是态控架构的核心目标，但 IN_SESSION（会话中）内用户要切换环节时，当前机制单靠模型概率判断 off-task（偏离），不能保证准确——模型可能漏判（把切换当成当前环节补充）或误判（把补充当成切换）。

**v4.7 的问题**：偏离检测缺少确定性兜底。

**v5.0 双重保障**（v5.1 修正：2026-07-17）：

```
保障1（主力——宪法强制 + 上下文管理器优先注入）：
  根宪法第1条（环节隔离）→ 模型每轮首要判断"是否本环节任务"
  → 上下文管理器将环节隔离声明（@section role 首位）以最高优先级注入 prompt
  → 模型每轮看到的上下文，第一件事就是"你当前在什么环节，什么是 off-task"
  → 不属于本环节 → 立即返回 off-task（优先级最高）
  → 大幅降低漏判率

保障2（辅助——ANALYZING 重新识别验证）：
  模型返回 off-task 后，调度器不直接路由
  → DET 只做非空检查（门卫，不做语义验证——DET 做不了语义判断）
  → 送回 ANALYZING 重新识别 offTaskInput
  → ANALYZING 的重新识别结果就是验证：
    → 识别出不同 intent → 确认切换，进入新环节
    → 识别出 same intent 或 other → 尊重模型判断，引导重新表达
  → v5.1 修正原因：LLM 自由生成的 confidence 不可靠（幻觉式自信），DET 无法做语义验证
```

**显式切换口令（M1 switch，v5.1 新增，v5.2 强化：S3 物理隔离）**：
用户可主动说"切断房间"口令 → DET 硬匹配（EXACT_MATCH，0 次 LLM）→ **调度器触发 S3 物理机制**：

1. **释放当前房间 KV Cache**：内存管理模块向底层推理引擎发送显式内存回收指令（`abort_request(roomId)`），当前房间的 KV Cache 内存块引用计数清零，交还给 GPU 显存分配器。
2. **归档当前房间**：contractStore 记录当前房间的 sessionCheckpoint + roomConversationLog。
3. **创建新推理会话**：新房间的 LLM 推理会话独立创建，从 contractStore 读取对应房间的 checkpoint（如存在），不继承任何旧房间的 KV 缓存。
4. **窗口延续**：前端不刷新、不跳转——用户在同一个界面，只是背后推理引擎换了一个会话。

显式口令是首选切换机制（100% 确定性），off-task 检测是兜底（用户忘了口令时的保底）。

**这是态控架构"房间"比喻的物理落地**——传统智能体需要用户新开窗口来切换上下文，态控架构在单一窗口内通过 S3 物理隔离实现房间自由切换。不是"请忽略之前的对话"（prompt 层面的请求），而是"之前的对话在物理上已不存在于 GPU 显存中"。

**全窗口房间状态索引（roomStateIndex，v5.3 新增）**：

S3 物理隔离解决了"新旧不串"的问题，但没有解决"切换后不知道旧房间有什么没做完"的问题。roomStateIndex 填补这个缺口：

- **数据来源**：contractStore 按 userId 跨房间查询所有 sessionCheckpoint + roomConversationLog 的最新状态快照
- **数据结构**：`[{roomId, roomName, intent, taskType, lastActiveAt, pendingCount, completedCount, lastSummary}]`
- **注入时机**：用户说"切断房间" → 调度器在 S3 物理释放前，先请求 context-manager 的 `buildRoomIndex()` 生成全窗口房间状态快照 → 连同新房间的 checkpoint 一起注入 ANALYZING 的 prompt
- **注入内容**：ANALYZING 收到的 prompt 末尾追加一个 `[全窗口房间状态]` 段落，列出所有房间的状态摘要（如"记账房间：1 条半成品待完成"、"提醒房间：3 条提醒待触发"）
- **意图识别增强**：ANALYZING 有了全局视野后，可以针对性地引导用户——"您记账房间还有一条未完成的记录，要先处理吗？"——而不是简单地返回 intent=other

**roomStateIndex 与 roomConversationLog 的区别**：

| | roomConversationLog | roomStateIndex |
|------|------|------|
| 粒度 | 单房间全量对话原文 | 全窗口房间摘要 |
| 用途 | IN_SESSION 环节内上下文拼接 | ANALYZING 跨房间意图识别 |
| 消费方 | context-manager.buildPromptContext() | context-manager.buildRoomIndex() |
| 更新频率 | 每轮追加 | 房间切换时 + sessionCheckpoint 变更时增量更新 |
| 存储 | contractStore.roomConversationLog | contractStore.roomStateIndex（独立表，userId 索引） |

**交互示例**：
```
用户："切断房间"
  → S3 释放记账房间 KV Cache
  → buildRoomIndex() 查询：记账房间 pending=1，提醒房间 pending=3
  → ANALYZING prompt 末尾注入：
    [全窗口房间状态]
    记账房间：半成品 1 条（7月15日记了半条"午饭25块"）
    提醒房间：待触发 3 条（最近：明天下午3点"开会"）
  → ANALYZING 返回 intent=reminder + suggestion="提醒房间有 3 条待触发提醒"
  → 用户可以选择："先去提醒房间" 或 "继续记账"
```

**设计原则**：不追求 100% 拦截，而是"降低漏判 + 拦住误判 + 回溯兜底"。用确定性兜底追求 100% 反而会破坏环节自治。

**与 v4.7 的关系**：v4.7 的第 0.5 层关键词扫描（只标记不判断）保留，作为保障 1 的辅助信号。v5.1 将保障 2 从"DET 事后验证"改为"ANALYZING 重新识别验证"——诚实面对 DET 做不了语义判断的事实。

#### 3.1.2 ANALYZING 的环节隔离（v5.0 新增，v5.1 扩展：双维度分类）

ANALYZING（分析态）是"意图识别环节"，受根宪法第 1 条约束：
- **本环节任务**：将用户输入映射到已知 intent（意图），同时分类输入性质。v5.1 扩展：ANALYZING 用结构化 prompt 执行两个并行的强制选择分类：

  **维度1：意图匹配**（v5.0 已有）——从有限选项中选择（如 A/B/C/D/E），开启 API logprobs 获取真实概率分布。返回 choice（选项字母）+ logprobs（概率分布）。DET 读取 logprobs 做阈值裁决（probability ≥ threshold → 放行，< threshold → intent=other）。

  **维度2：输入性质**（v5.1 新增）——判断输入属于哪类：
  - S) 闲聊/松弛：无明确目标，随意表达，社交寒暄
  - T) 任务导向：有明确目标，结构化表达，包含动作意图
  - U) 不确定

  ANALYZING 一次调用返回两个信号：
  ```
  { choice: "C", logprobs: {A:0.05, B:0.03, C:0.78, D:0.08, E:0.06},
    inputNature: "T", inputNatureLogprobs: {S:0.02, T:0.91, U:0.07} }
  ```

  DET 交叉裁决路由：

  | intent match | input nature | 路由 |
  |-------------|-------------|------|
  | A-E（高 probability） | — | 对应正式房间 |
  | other（低 probability） | S（闲聊） | **SLACK_NODE** |
  | other（低 probability） | T（任务导向） | **TEMP_TOPIC** → 创建运行时房间 |
  | other（低 probability） | U（不确定） | 追问澄清 |

  **关键：一次 LLM 调用完成两个分类，不改模型，不追加调用。**只是 prompt 多问一个问题，返回多一个字段。

- **非本环节任务**：不能采集字段（那是 IN_SESSION 的事）、不能追问（那是 IN_SESSION 的事）、不能给出最终回复
- **边界情况**：输入无法映射到任何已知 intent → 返回 `intent=other`（低 probability）。注意：ANALYZING 只产出 intent + inputNature，不产出 turnType——turnType 是 IN_SESSION 的输出格式。

**ANALYZING 的产出物感知（v5.2 新增）**：ANALYZING prompt 末尾追加 `[全系统产出物]` 段落——从 outputRegistry 查询当前用户的最近产出物列表。这使 ANALYZING 在意图识别时就拥有全系统产出物视野，可以在路由建议中主动提示："元智能体房间有L2设计文档，需要引用吗？" 详见 §3.1.4。

#### 3.1.3 产出物总索引——Output Registry（v5.2 新增）

**定位**：全系统所有房间产出物的统一登记簿。与 roomStateIndex（房间状态摘要）互补——roomStateIndex 回答"房间有什么待办"，outputRegistry 回答"房间产出了什么"。

**存储**：contractStore 中新增 `outputRegistry` 表：

```sql
CREATE TABLE IF NOT EXISTS outputRegistry (
    outputId TEXT PRIMARY KEY,
    roomId TEXT NOT NULL,
    roomName TEXT NOT NULL,
    intent TEXT NOT NULL,
    taskType TEXT NOT NULL,
    outputType TEXT NOT NULL,       -- contract_json / document / report / code / patent_draft / ...
    outputPath TEXT NOT NULL,       -- 文件系统路径
    outputName TEXT NOT NULL,       -- 人类可读名称
    outputSummary TEXT,             -- 模型生成的摘要，供检索匹配
    keywords TEXT,                  -- JSON数组，关键标签
    fileSize INTEGER,
    createdAt TEXT NOT NULL,
    createdBy TEXT NOT NULL,
    status TEXT DEFAULT 'active',
    metadata TEXT                   -- JSON，扩展元数据
);
```

**索引**：`(roomId, createdAt)`、`(intent, outputType)`、FTS on `(outputName, outputSummary, keywords)`。

**写入时机**：

| 时机 | 操作 |
|------|------|
| IN_SESSION complete（field_based） | contract-out JSON 自动注册 |
| IN_SESSION complete（topic_based） | 用户确认的最终产出自动注册 |
| TEMP_TOPIC 用户触发总结 | 总结产出自动注册 |
| 用户手动导出/保存 | 调度器调用 registry.register() |

**检索方式**（DET 确定性查询，0 次 LLM）：

```
- 按房间：registry.findByRoom("元智能体")
- 按类型：registry.findByType("document")
- 按关键词：registry.search("L2 设计文档")
- 按时间：registry.findSince("2026-07-01")
- 组合：registry.search({roomName: "元智能体", outputType: "document"})
```

**跨房间产出物发现与复制流程**：

```
1. 用户在专利撰写房间说："调用元智能体之前生成的L2设计文档，作为专利的背景技术"
   → 第0.5层关键词扫描检测到"调用"+"元智能体"→ 标记 crossRoomRequest

2. 调度器查询 outputRegistry:
   registry.search({roomName: "元智能体", keywords: ["L2", "设计"]})
   → 返回匹配列表（含 outputName、outputSummary、createdAt）

3. 用户确认目标产出物

4. 调度器执行文件复制（DET）:
   cp {outputPath} → {currentRoomWorkspace}/references/{outputName}
   → 记录操作审计日志

5. 专利撰写房间的上下文管理器下一轮拼接时，自动将 references/ 目录文件列表
   注入房间上下文，模型可以读取引用文件的内容
```

**关键纪律——房间隔离不妥协**：

| 可以跨房间共享 | 不可以跨房间共享 |
|--------------|----------------|
| ✅ 结构化产出物文件（通过文件复制） | ❌ roomConversationLog（房间对话原文） |
| ✅ outputRegistry 元数据（产出物名称、摘要、路径） | ❌ 其他房间的领域规则 |
| ✅ roomStateIndex 状态摘要（每个房间有待办数） | ❌ 其他房间的 sessionCheckpoint 内部字段 |

文件复制是唯一的跨房间数据传递方式——不直接注入原文到上下文（防止上下文膨胀），而是复制文件到目标房间的 workspace/references/ 目录。目标房间的上下文管理器在下一轮拼接时，按需要决定是否将引用文件内容注入当前上下文。**房间之间的对话原文仍然完全隔离。**

**与 roomStateIndex 的互补**：

| | roomStateIndex | outputRegistry |
|------|------|------|
| 粒度 | 房间级摘要 | 产出物级明细 |
| 回答什么 | "记账房间还有1条半成品" | "元智能体产出了3份文档、2份代码" |
| 消费方 | ANALYZING（跨房间切换建议） | ANALYZING（跨房间产出发现）+ 调度器（文件操作） |
| 更新频率 | 房间切换时 + checkpoint 变更时 | 每次产出物注册时 |

**交互示例**：

```
用户（在专利撰写房间）："引用元智能体的L2设计文档，然后基于它写专利的技术背景"
  → ANALYZING: intent=patent_write, crossRoomRequest=true
  → 调度器查询 outputRegistry → 匹配到 "L2设计文档_v2.0.md"
  → 调度器："找到元智能体的L2设计文档（2026-07-15，12KB），要引用吗？"
  → 用户确认
  → 调度器: cp L2设计文档_v2.0.md → 专利撰写房间/references/
  → 上下文管理器注入 references/ 文件列表
  → IN_SESSION(patent_write):
     "基于 references/L2设计文档_v2.0.md，请撰写发明专利的技术背景部分..."
```


### 3.2 taskType 双模式

**区分原点：收敛空间的性质，而非任务内容。**

taskType 是态控架构所有设计决策的逻辑起点——上下文拼接方式、校验方式、记忆载体、规则匹配键、追问策略、模型选择策略，全部收敛到这一个二分法上。

**field_based（基于字段）——离散参数空间，定位唯一正确值**

收敛空间是离散的、可枚举的参数集合。目标是从所有可能的候选中定位唯一正确值。存在客观正确答案——找到了，任务完成；没找到，任务未完成。正确与否可被 DET 值域硬校验验证。

典型场景：记账（从所有可能的类别中找到"猫粮"，从所有可能的金额中找到"35"）、改柜体参数（从所有可能的深度值中找到"600mm"）、查询（从所有可能的记录中找到"上周三的订单"）、删除（从所有可能的条目中找到"第5条"）、表单填写。

**topic_based（基于主题）——连续语义空间，采样满足约束的样本**

收敛空间是连续的、不可枚举的语义空间。目标是在受约束的主题空间内采样一个满足条件的样本。不存在唯一正确答案——同一主题下，可以生成无数个不同的合格输出。无法用"对/错"评判，只能用"是否在主题边界内 + 是否满足格式约束"来判断。

典型场景：写诗（在"秋天"主题空间内生成一首满足七言绝句格式的样本）、写专利（在"一种基于XXX的方法及系统"空间内生成满足专利撰写规范的样本）、角色扮演（在"林霜是侦探助手"空间内生成符合角色设定的对话）、头脑风暴（在"如何提升用户留存"空间内生成一组可行的想法）。

**全维度差异表：**

| 维度 | field_based（基于字段） | topic_based（基于主题） |
|------|----------------------|----------------------|
| 收敛空间 | 离散参数空间（可枚举） | 连续语义空间（不可枚举） |
| 目标 | 定位唯一正确值 | 采样满足约束的样本 |
| 是否存在客观正确答案 | 是 | 否 |
| 验证方式 | 值域校验（DET 硬规则） | 格式校验 + 主题边界检查（DET + 语义判断） |
| 追问方式 | 按预定义字段顺序追问 | 按主题探索路径追问（模型自主决定） |
| 完成条件 | 所有必采字段齐全 | 输出满足主题边界 + 格式约束 |
| 跨节点传递载体 | contract-out JSON（字段集）+ sessionCheckpoint（进度快照） | topicEvolution（事件序列 + 状态快照） |
| 长期记忆 | sessionCheckpoint（进度快照）+ roomConversationLog | topicEvolution（主题演化历史） |
| 领域规则匹配 | stepName DET 精确匹配 | topicPath 语义匹配 |
| 错误形态 | 值错了（可 DET 校验） | 跑偏了（需语义判断） |

**混合型任务：主 taskType + 子约束**

某些任务同时跨越两种空间。区分标准不是"任务里有没有字段"，而是"收敛空间的主体是连续的还是离散的"：
- 主体是连续语义空间（如创作），但带有离散约束（如格式）→ topic_based 主 + field_based 子。例："写一首关于秋天的七言绝句"——诗歌创作是 topic_based，但"七言绝句"的格式约束（28字、平仄押韵）是 field_based。DET 复验时先做 field_based 格式校验，再做 topic_based 主题边界校验。
- 主体是填写字段（如录入客户信息），但带有自由文本（如备注）→ field_based 主 + topic_based 子（备注字段不做值域校验，只做主题边界检查）。

主 taskType 决定跨节点载体和长期记忆机制（topic_based 主→topicEvolution；field_based 主→sessionCheckpoint），子约束附加在 DET 复验的末端校验中。长期记忆载体统一为 contractStore——field_based 走 contractStore.sessionCheckpoints，topic_based 走 contractStore.topicEvolution。

**topicEvolution 机制：**

topicEvolution机制是态控架构为topic_based任务设计的标配跨节点传递载体。

机制本体分三层递进。

A最底层是分层留存，按信息净价值把每个事件判为major、minor或invalid三级，major永久保留，minor超阈值后汇总压缩，invalid直接归档不入主表，判定权交还给模型在turnType中顺带输出，DET不参与语义判定。三级判定基于用户在交互中的明确行为信号——例如版本号从1.0改为2.0是major，从2.0改为2.1是minor，用户明确说"这个内容不用，取消"是invalid。判定依据是用户交互中的明确行为，不是模型的元认知自省。

B中间层是主题关联，在topicIndex中建立八种关联关系，核心纪律是"关联在元数据层，隔离在数据层"，注入时只拉摘要卡片不拉原始事件链，模型知道隔壁主题的结论但不知道隔壁主题的聊天记录。

C最顶层是异步规则提炼，从多条同类主题的演变流程和关联关系中提取被多次验证的行为模式，候选规则经交叉验证和用户确认后存入领域规则库，同类型新主题初始化时自动注入。规则提炼应为低频行为。

**领域规则体系**：规则不再只是平铺的"行为模式列表"，而是一个包含权限分层（L0-L3）、来源分类（scenario_seed/user_initiated/model_inferred）、关联边（depends_on/conflicts_with/strengthens）和条件匹配（conditions）的规则网络图。注入方式"按 topicPath 子图匹配精准注入"。

---

**sessionCheckpoint 是 field_based 的跨节点传递载体**

```sql
sessionCheckpoints(userId, lastCompletedStep, completedSteps[], stepSnapshots{})
```

| 字段 | 含义 | 谁写 |
|------|------|------|
| lastCompletedStep | 最后一个完成的环节名 | 每 complete → 态控调度器更新 |
| completedSteps | JSON 数组，已完成环节列表 ["p0","n1","n3"] | 同上 |
| stepSnapshots | JSON 对象，每环节收敛时的结构化字段快照 {"n1": {collectedFields: {...}, timestamp: "..."}} | 同上 |

三种写入时机：

| 事件 | 操作 |
|------|------|
| 环节 complete | completedSteps 追加，stepSnapshots[环节名] 写入 collectedFields，lastCompletedStep 更新 |
| 环节 complete（覆盖已有快照） | stepSnapshots[环节名] 覆盖——回到已完成的房间，重做后快照更新 |
| ttl 到期 | 整行删除，联动清理 roomConversationLog |

两种读取路径：

| 路径 | 触发 | 谁读 |
|------|------|------|
| ANALYZING 恢复 | 用户唤醒，DET 查 checkpoint 是否存在+是否过期 | 态控调度器——决定"恢复继续还是从 P0 开始" |
| 上下文拼接 | 进入某个已完成的环节时 | 上下文管理器——读 stepSnapshots[环节名] 的 collectedFields |

和 topicEvolution 的对位：

| 维度 | topicEvolution（topic_based） | sessionCheckpoint（field_based） |
|------|----------------------|----------------------|
| 载体形式 | 事件流：每节点 exit 追加 event | 进度标记：每节点 complete 更新一行 |
| 匹配方式 | topicId embedding 语义检索 | userId DET 精确匹配 |
| 记录粒度 | 事件级——每 turn 的事件序列 | 环节级——每环节一帧快照 |
| 注入给模型 | 三层注入（已生效规则+关联主题摘要+演化事件包） | 结构化字段 collectedFields |

---

**环节内规则与跨主题规则的优先级与冲突仲裁：**

domainRules 表中存在两类规则来源：
- **环节内规则（纵向）**：来自单环节的 changeLog 提炼，反映特定环节的稳定行为模式
- **跨主题规则（横向）**：来自多条同类主题的 topicEvolution 交叉验证，反映跨主题的通用行为模式

两层规则都写入 domainRules 表，优先级与冲突仲裁定义如下：

| 维度 | 规则定义 |
|------|---------|
| 优先级 | 环节内规则 L2 > 跨主题规则 L2（同优先级时环节内优先，因为更具体） |
| 冲突仲裁 | 冲突时不允许自动覆盖，标记冲突后进入人工确认队列 |
| 覆盖关系 | 高优先级规则不自动覆盖低优先级规则，而是标记冲突等待人工裁决 |
| 共存关系 | 不冲突的环节内规则与跨主题规则可共存，注入时按优先级排序拼接 |

**设计依据**：环节内规则更贴近具体场景，可信度更高；跨主题规则覆盖面更广，但抽象层级更高。两者冲突时，自动覆盖会丢失其中一方的信息，交给人工确认可保留裁决可追溯性，符合"人作为终极容错闭环"的铁律。

---

贯穿三层有两条铁律：一是人作为终极容错闭环，系统自动发现、自动判定、自动提炼，但主题名、关键词、关联关系、领域规则全部必须经用户确认后才生效；二是认知局部性不因主题关联而妥协，关联主题的原始数据不注入当前上下文，只注入经过提炼的结论摘要。整个机制的本质不是存储空间管理，是按信息净价值做筛选——大版本保全过程理解，小版本防膨胀，无效过程减噪声，关联在元数据层不混不孤立，规则异步沉淀越用越老练。

### 3.3 混合型任务

某些任务可能同时包含两种模式。例如"写一首关于秋天的七言绝句"——诗歌创作本身是 topic_based（基于主题），但"七言绝句"的格式约束（28字、平仄押韵）是 field_based（基于字段）。处理方式：在环节宪法（环节宪法）中声明主 taskType（任务类型）（topic_based（基于主题）），将 field_based（基于字段） 约束作为 topic_based（基于主题） 的格式约束子条款。DET 复验时先做 field_based（基于字段） 格式校验，再做 topic_based（基于主题） 主题边界校验。
## 3.4 三类执行体分派（DET / LLM / HUMAN）

**3.4.1 分派判据**

每个状态在宪法中必须显式声明执行体，判据顺序如下（优先级递减）：

A. 错误代价多大？
代价大且无客观答案 → 人
代价大但有客观校验 → LLM + 人确认
代价小 → 进入下一条

B. 能否写成硬规则 + 穷举？
能 → 确定性程序（DET）
不能 → 进入下一条

C. 是否有客观正确答案？
有（映射类、意图类）→ LLM
没有（偏好类、责任类）→ 人

关键规则："代价"为最高优先级判据。代价大的节点无论能否硬规则化，都必须经过人确认或 LLM+人确认。

**3.4.2 与意图收敛机制的对应**

DET 执行确定性收敛；LLM 执行有限展开后收敛，出口被 contract-out schema（输出契约模式） 硬卡；人执行责任收敛，对不可逆决策负责。

### 3.5 增强分类

规模增强（scale up（扩展） 才需要）：并联实例化、长上下文显式管理。

### 3.6 人和DET作为终极容错闭环

态控不追求大模型的单次绝对正确，而是构建"犯错-拦截-纠错"的工程闭环。用户不仅是意图源头，更是系统状态的"重置按钮"。系统只需保证"大概率正确"，剩余边界错误由DET和用户纠正完成最终收敛。在高风险场景中，这一闭环从单用户现场扩展到组织级人工接管通道。

### 3.7 领域规则体系（v4.2 新增）

**定位**：领域规则是态控架构的第三层宪法（领域规则层）在生产过程中逐步建立的约束体系。与宪法不同——宪法是设计者预设的刚性框架，领域规则是从用户的实际使用中"长出来"的经验沉淀。v4.2 将领域规则从平铺列表升级为可查询、可冲突检测、可按主题路径匹配的**规则网络图**。

**field_based 的等价物——contractEvolution**：topic_based 的规则按 topicPath（行业→品类→场景）分层，field_based 的规则按 stepName（环节名）分层——规则提炼和注入完全按环节隔离。两者机制相同（changeLevel→ruleMiningQueue→异步提炼→domainRules），唯一差异是匹配键：topic_based 用 topicPath 语义检索，field_based 用 stepName DET 精确匹配。

#### 3.7.1 规则来源三层

领域规则有三类来源，对应不同的可信度和确认流程：

| 来源 | 含义 | 灌入时机 | 权限初始值 | 是否走交叉验证 |
|------|------|---------|-----------|-------------|
| **scenario_seed** | 场景固有规则（工艺规范、设计基线、报价模板、安全红线等） | 主题初始化时按 topicPath 加载 | L0 / L1 | ❌ 不走，设计者预设可信 |
| **user_initiated** | 主题确认时用户主动口述的规则（如"厨房台面我喜欢高一点"） | ANALYZING 确认 topicName 后主动问询 | L2 | ✅ 走，用户可能说错或矛盾 |
| **model_inferred** | IN_SESSION 过程中模型推断的用户偏好（如"从对话中推测用户偏好 U 型厨房"） | complete 时顺带输出 → ruleCandidates | L3 | ✅ 需用户确认升格为 L2 |

#### 3.7.2 规则权限分层

领域规则内部存在 L0-L3 四级权限梯度。这一维度**与宪法层级正交**——宪法分四层（根/公共/局部/领域），每条领域规则同时具有"宪法层级"（属于第 3 层领域规则）和"权限层级"（L0/L1/L2/L3），分别定义其适用范围和可修改性。

| 层级 | 名称 | 含义 | 可修改 | 默认 importance | 冲突时行为 |
|------|------|------|--------|-----------------|----------|
| **L0** | 钢印规则 | 场景硬性约束，不可触碰 | ❌ 不可修改 | critical（L0 规则默认 critical，受"同层 critical≤1"约束——L0 层内必须用户确认选哪条为 critical，详见 §3.10） | 无条件胜出，不询问 |
| **L1** | 基线规则 | 设计者预设最佳实践（如"厨房台面高度建议 85cm"） | ⚠️ 需设计者确认 | normal | 与 L2/L3 冲突时触发询问 |
| **L2** | 用户确认规则 | 用户主动说过或确认过的偏好 | ✅ 用户可随时修改 | normal | 优先于 L3 |
| **L3** | 模型推断规则 | 模型 infer 的候选规则 | ✅ 需用户确认升格 | normal | 仅作参考，不主动注入 |

**importance 与 immutableLevel 正交**：
- immutableLevel 是权限轴（谁能改），importance 是重要程度轴（哪个更关键）。同 immutableLevel 内，importance 高的胜出
- 每层（L0/L1/L2/L3）内 critical ≤ 1、high ≤ ⌊该层规则总数/3⌋、其余 normal
- L0 规则中必须选出 1 条 critical（安全红线——用户确认），其余可全 normal
- 冲突仲裁：先比 immutableLevel → 同层再比 importance → 同 importance 触发询问

**跨层仲裁的边界声明（v4.6 新增）**：
- 当两条规则 immutableLevel 不同时（如 L0 vs L2），按 immutableLevel 直接裁决（L0 无条件胜出），**importance 不参与跨层仲裁**
- importance 的作用域限定为"**同 immutableLevel 平局时的次级裁决**"——只有在两条规则 immutableLevel 相同时，importance 高的才胜出
- 示例：L0(normal) vs L2(critical) 冲突时，L0 直接胜出，L2 的 critical 不参与仲裁；L2(critical) vs L2(normal) 冲突时，L2(critical) 胜出
- 此声明消除"importance 可以跨层影响仲裁"的误解，避免实现者误以为"高 importance 的低权限规则能压过低 importance 的高权限规则"

**规则状态**：

| 状态 | 含义 | 所在位置 |
|------|------|---------|
| active | 正式生效的规则 | domainRules |
| candidate | 待交叉验证的候选规则 | ruleCandidates |
| archived | 已废弃或被替代的历史版本 | topicEvolutionArchive |
| pending_review | 需人工审核（L0/L1 修改请求） | 单独队列 |

#### 3.7.3 主题层级与规则的作用域边界

topicPath 是一个**层级化数组**，从粗到细逐级展开。每一层都可以有自己独立的规则集——不同层级的规则互不污染。

**层级结构示例**：

```
全屋定制
├── 设计
│   ├── 厨房设计     ← 规则："台面高度85cm""水槽位置""油烟机型号偏好"
│   ├── 卫生间设计   ← 规则："防水等级""瓷砖规格""干湿分离要求"
│   └── 客厅设计
├── 报价             ← 规则："利润率""套餐模板""折扣权限"
├── 客户沟通
├── 拆单
└── 生产
```

**层级匹配规则（前缀匹配）**：

| 当前 topicPath | 能匹配到哪些规则 | 不能匹配到 |
|--------------|---------------|----------|
| `["全屋定制"]` | topicPath 前缀为 `["全屋定制"]` 的所有规则——包括"设计"、"报价"、"生产"等全部子层规则 | — |
| `["全屋定制", "设计"]` | 前缀为 `["全屋定制", "设计"]` 的规则——包括"厨房设计""卫生间设计""客厅设计" | `["全屋定制", "报价"]` 的规则（兄弟节点不可见） |
| `["全屋定制", "设计", "厨房设计"]` | 仅 `["全屋定制", "设计", "厨房设计"]` 自身的规则 | "卫生间设计""报价"等的规则（隔层不可见） |
| `["全屋定制", "报价"]` | 仅 `["全屋定制", "报价"]` 自身的规则 | "厨房设计"等的规则 |

**核心纪律**：
- **上级可见于下级**：`["全屋定制", "设计"]` 层的规则对 `["全屋定制", "设计", "厨房设计"]` 可见——因为前者是后者的前缀。这意味着在"厨房设计"环节，模型能拿到"设计"层的通用规则（如"全屋风格统一为现代简约"）+ "厨房设计"层的专用规则（如"台面高度85cm"）
- **下级不可见于上级/兄弟**：`["全屋定制", "设计", "厨房设计"]` 的规则不会注入到 `["全屋定制", "设计", "卫生间设计"]` 或 `["全屋定制", "报价"]` 的上下文中。厨房台面高度对报价毫无意义
- **层级深度与上下文窗口的关系**：当前 topicPath 越深，匹配到的规则集越小——这是认知局部性原则在规则注入层的自然体现

**规则提炼时的作用域要求**：

每一轮对话和每一个提炼出来的规则都必须带 topicPath 和 source 标记。模型推断的候选规则（model_inferred）在写入 ruleCandidates 时，必须附带当前对话所处的 topicPath，否则规则无法在后续对话中被正确匹配。

```json
{
  "rule": "厨房台面高度建议85cm",
  "scope": {
    "topicPath": ["全屋定制", "设计", "厨房设计"],
    "conditions": {
      "userHeight": ">=170cm"
    }
  },
  "source": "user_initiated",
  "immutableLevel": "L2",
  "status": "candidate"
}
```

#### 3.7.4 规则网络结构

领域规则不再是平铺的列表，而是一个有向图——每条规则是一个节点，节点之间通过关联边建立关系。

**节点定义**：

```json
{
  "ruleId": "kitchen_countertop_height_001",
  "content": "厨房台面高度建议85cm",
  "topicPath": ["全屋定制", "设计", "厨房设计"],
  "source": "scenario_seed",
  "immutableLevel": "L1",
  "importance": "normal",
  "status": "active",
  "conditions": {
    "userHeight": ">=170cm"
  },
  "edges": {
    "conflicts_with": ["kitchen_countertop_height_002"],
    "depends_on": [],
    "strengthens": ["ergonomic_guideline_003"]
  },
  "version": 1,
  "createdAt": "2026-07-09T10:00:00Z",
  "updatedAt": "2026-07-09T10:00:00Z"
}
```

**关联边类型**（统一 snake_case + 主动语态）：

| 边类型 | 含义 | 注入时行为 |
|--------|------|----------|
| **depends_on** | A 规则成立的前提是 B 规则已满足 | 匹配 A 时，检查 B 是否已激活；若否，主动询问 B |
| **conflicts_with** | A 和 B 互斥，不能同时应用 | 同时匹配到 A 和 B 时，触发询问让用户二选一 |
| **strengthens** | A 规则强化 B 规则的可信度 | 匹配到 A 时，B 的权重 +1，同层级内优先注入 |

**命名纪律**：关联边统一使用 snake_case（`depends_on` / `conflicts_with` / `strengthens`），语义统一为主动语态——当前规则是动作的发起者。`strengthens` 表示"当前规则强化了目标规则"，不是"被目标规则强化"。

#### 3.7.5 规则生命周期

```
主题确认（ANALYZING）
  │
  ├── 加载场景种子规则（scenario_seed）→ domainRules
  ├── 主动询问用户规则（user_initiated）→ ruleCandidates
  │
  ▼
IN_SESSION 运行
  │
  ├── 每轮：按 topicPath 做规则网络子图匹配 → 精准注入
  ├── 遇到冲突或不满足条件 → 主动询问用户
  ├── 模型 infer 出新规则（model_inferred）→ ruleCandidates
  │
  ▼
环节结束（complete / giveup / off-task）
  │
  ├── checkpoint → 推入 ruleMiningQueue
  ├── abandoned → 推入 ruleMiningQueue
  │
  ▼
N10 后台异步处理
  │
  ├── 压缩 stateChange 摘要（轻量 LLM）
  ├── 提取候选规则（继承 immutableLevel）
  ├── 自动推断规则间关联边（conflicts_with / strengthens / depends_on）
  │
  ▼
用户确认 → 升格为正式规则 → 写入 domainRules
```

#### 3.7.6 规则注入匹配逻辑（context-manager 第0层改造）

**v4.0 原逻辑**：全量读取 domainRules → 一股脑注入

**v4.2 新逻辑**：按 topicPath 做子图匹配 → 精准注入

每轮用户输入的处理流程：

1. 获取当前 topicPath（如 ["全屋定制", "设计", "厨房设计"]）
2. 在 domainRules 中做子图匹配：
   - 匹配 topicPath 前缀一致的规则节点
   - 沿 depends_on 和 strengthens 边向外扩一层
   - 检查 conditions 是否满足
3. 冲突检测：
   - 若匹配到的规则中有 conflicts_with 关系的节点 → 触发询问
   - 按 immutableLevel 决定询问策略（L0 直接覆盖，L1 推荐 vs L2 用户偏好）
4. 条件不满足检测：
   - 若匹配到的规则 conditions 字段有未满足的条件 → 主动询问补充
5. 注入 prompt：
   - 按优先级：L0 > L1 > L2 > L3
   - **同层级内按 importance 排序：critical > high > normal > low**
   - 同 importance 内按 strengthens 边的权重排序
   - 同 importance 同 strengthens 权重时，按 ruleId 字典序（确定性 fallback，避免同分乱序）

**strengthens 权重累加上限（v4.6 新增）**：
- 同一目标规则被多条 strengthens 边指向时，权重累加上限为 **+3**（cap）
- 累加超过 +3 的部分不再计入排序权重——避免一条规则被 N 条 strengthens 加权后垄断排序首位
- cap 值通过 tunable 参数 `strengthens_weight_cap`（默认 3，取值范围 [1, 10]）配置，由上下文管理器消费

**注入包格式**：

```json
{
  "matchedRules": [
    {
      "ruleId": "...",
      "content": "...",
      "immutableLevel": "L1",
      "matchType": "exact_topicPath"
    }
  ],
  "conflictsDetected": [
    {
      "ruleA": "rule_id_1",
      "ruleB": "rule_id_2",
      "resolution": "pending_user_choice"
    }
  ],
  "missingConditions": [
    {
      "ruleId": "...",
      "condition": "userHeight",
      "question": "您的身高大概多少？这样我能更精准地建议台面高度。"
    }
  ]
}
```

**与认知局部性的关系**：子图匹配取代了 v4.0 的全量注入——模型不再收到"这个领域的所有规则"，而是只收到"当前主题路径上直接相关的那几条规则及其直接关联的规则"。这使上下文窗口占用从 O(领域规则总数) 降为 O(当前主题路径规则的子图规模)，是认知局部性原理在领域规则层的落地。

#### 3.7.7 规则提炼队列处理

```
checkpoint / abandoned
  │
  ▼
推入 ruleMiningQueue
  │
  ▼
N10 后台进程消费
  │
  ├── 压缩 stateChange 摘要（轻量 LLM）
  ├── 提取候选规则（model_inferred，L3）
  ├── 自动推断关联边：
  │   ├── 若两条规则 topicPath 相同且内容矛盾 → conflicts_with（标记为 inferred_conflict 候选状态，v4.6 新增）
  │   ├── 若一条规则引用了另一条的结果 → depends_on
  │   └── 若一条规则与另一条方向一致 → strengthens
  │
  ▼
写入 ruleCandidates
  │
  ▼
等待用户确认
  │
  ├── 确认 → 升格为 L2，写入 domainRules
  ├── 修改 → 按用户修改后写入，标记 L2
  └── 否决 → 丢弃或标记 rejected
```

**inferred_conflict 候选状态纪律（v4.6 新增）**：

| 状态 | 含义 | 生命周期 | 是否参与运行时冲突检测 |
|------|------|---------|---------------------|
| `inferred_conflict` | 自动推断的冲突关系候选，待用户确认 | N10 自动推断后写入；用户确认前 | ❌ 不参与——避免未确认的 LLM 语义判断直接影响运行时仲裁 |
| `active_conflict` | 经用户确认生效的正式冲突关系 | 用户确认 inferred_conflict 后升格；写入 domainRules 的 edges 字段 | ✅ 参与——§3.7.6 注入匹配逻辑触发询问 |

**纪律依据**：自动推断"两条规则内容矛盾"本身就是 LLM 语义判断，引入了新的概率性环节。直接将推断结果作为 active_conflict 注入运行时冲突检测，会让"确定性优先"的架构立场在规则网络层被悄悄破坏。强制候选状态 + 用户确认升格，把概率性环节封在异步提炼阶段，运行时仍只消费确定性结果。

**降级处理**：若 inferred_conflict 长期未确认（超过 ruleMiningQueue 的处理周期），不应自动升格为 active_conflict，也不应直接丢弃——应保留为 inferred_conflict 状态，并在下一轮用户确认窗口中提示。

**交叉验证策略按来源区分**：
- scenario_seed（L0/L1）：不走交叉验证，设计者预设可信
- user_initiated（L2）：需交叉验证（用户可能口头说错或与既有规则矛盾）
- model_inferred（L3）：需交叉验证 + 用户确认升格

### 3.8 跨会话记忆机制（v4.3 新增）

态控架构为两种 taskType 分别提供跨会话记忆机制：

| 模式 | 机制 | 匹配方式 | 核心存储 |
|------|------|---------|---------|
| topic_based | topicEvolution | topicId 语义锚点 + embedding 检索 | 四表（topicEvolution/topicEvolutionEvent/topicEvolutionArchive/topicIndex） |
| field_based | sessionCheckpoint | userId 精确匹配 + DET ttl 检查 | 单表（sessionCheckpoints） |

#### 3.8.1 topicEvolution（topic_based 专属）

主题演化体系——基于 topicId 的语义锚点，支持三层注入（已生效领域规则 + 关联主题摘要 + 分层演化事件包）、异步规则提炼、changeLevel 分层留存（major 永久 / minor 压缩 / invalid 归档）。详见 §3.2、§3.7。

#### 3.8.2 sessionCheckpoint（field_based 专属，v4.3 新增）

field_based 模式的原生跨会话进度记忆——记录用户在一个多步骤流程中的进度和各步骤已采集字段快照。

**存储结构**：

```sql
CREATE TABLE IF NOT EXISTS sessionCheckpoints (
    userId TEXT PRIMARY KEY,
    lastCompletedStep TEXT NOT NULL,
    completedSteps TEXT NOT NULL,    -- JSON: ["p0","n1","n3",...]
    stepSnapshots TEXT NOT NULL,     -- JSON: {"n1":{"collectedFields":{...},"timestamp":"..."}}
    resumedAt TEXT,
    expiredAt TEXT,
    ttl INTEGER DEFAULT 604800      -- 秒，默认 7 天
);
```

**调度器恢复流程**：

```
1. 用户唤醒
2. ANALYZING 阶段 DET 查 sessionCheckpoints
   ├─ 无记录 ∥ 已过期 → 正常进 P0
   └─ 有记录且未过期 →
        生成 resume_chat intent → 询问用户"上次做到 X，继续还是去其他环节？"
        ↓
        确认"继续" → 注入 completedSteps + stepSnapshots
                    N4 找第一个未完成步骤 → 路由到该步骤
        用户说"去 Nj" → 校验 Nj 前置依赖全部已满足
                    → 注入 stepSnapshots["nj"]（Nj 上次收敛快照）
                    → 路由到 IN_SESSION(nj)
        用户拒绝 → 清空 checkpoint，正常进 P0
```

**房间比喻**：每个环节是独立房间——返回 N1 时只注入 N1 的快照，不会混入 N10 的内容。

#### 3.8.2.2 contractEvolution：field_based 的领域规则演化

contractEvolution 是 §3.7 领域规则体系在 field_based 模式下的具体形态。与 topic_based 的 topicEvolution 不同——topicEvolution 管"主题怎么演化"，contractEvolution 管"环节内部的设计规则怎么变"。

**存储**（复用 contractStore 现有表，按 stepName 隔离）：

| 表 | 用途 | field_based 行为 |
|----|------|----------------|
| domainRules | 已生效领域规则 | ruleId 含 stepName 前缀，按 stepName DET 精确匹配注入 |
| ruleCandidates | 待验证候选规则 | 来源：每个 IN_SESSION complete 时模型推断的用户偏好 |
| ruleMiningQueue | 提炼队列 | 按 stepName 分队列——MiningQueue 的每条记录标注 stepName，异步提炼按 stepName 分组处理 |

**changeLevel 判定规则（field_based）**：

| 级别 | 判定标准 | 留存 |
|------|---------|------|
| major | 字段的结构性变更——新增/删除必采字段、修改字段值域、改变 Schema 结构 | 永久保留，不可压缩 |
| minor | 字段值微调——默认值修改、取值范围调整、措辞优化 | 30 条压缩为 1 条摘要 |
| invalid | 完全撤销——用户改了又改回原值（与上一版 contract-out 完全相同） | 归档后定期删除（90 天） |

**三层注入**（由上下文管理器执行）：

| 层 | 内容 | 匹配方式 |
|----|------|---------|
| 第0层 | 已生效领域规则 | stepName 精确匹配 + immutableLevel 过滤（L1 及以上） |
| 第1层 | 候选规则卡片 | stepName 匹配 + validationRate ≥ 阈值 |
| 第2层 | 变更历史 | 同一 stepName 的近期变更摘要（major 3条 + minor 压缩摘要 + 当前规则状态快照） |

#### 3.8.2.3 contractEvolution 与 sessionCheckpoint 的关系

**设计原则**：
- 态控调度器只负责两件事：**全量记录** + **正确路由**。三个数据源按各自匹配键全量记录，不做注入决策——roomConversationLog 按 userId+roomId，sessionCheckpoints 按 userId，contractStore 按 stepName 或 topicPath
- 每个环节是自治的——它的上下文管理器从态控调度器交给它的内容中按自己的拼接规则组装 prompt。上下文管理器是独立状态机，不执行环节宪法
- 不与 topicEvolution 竞争——field_based 不需要语义锚点，userId 精确匹配足够
- ttl 到期自动清除——设计假设：7 天内没继续的任务大概率被放弃了

**与 contractEvolution 的关系**：

| | contractEvolution | sessionCheckpoint |
|---|---|---|
| 记录什么 | 规则变更历史 | 流程进度 |
| 匹配方式 | stepName 精确 | userId 精确 |
| 留存策略 | major 永久/minor 压缩/invalid 归档 | ttl 过期清除 |
| 消费方 | 上下文管理器（三层注入） | 态控调度器（ANALYZING 路由） |

#### 3.8.3 跨会话记忆对比

| 维度 | topicEvolution | sessionCheckpoint | contractEvolution |
|------|---------------|-------------------|-------------------|
| 适用 taskType | topic_based | field_based | field_based |
| 记录内容 | 主题演化历史 | 流程进度 + 字段快照 | 环节内规则变更历史 |
| 存储规模 | 4 表 + topicIndex 9 张 | 1 表 | 复用 contractStore 3 表 |
| 检索方式 | embedding 两阶段 | DET 精确匹配 | DET stepName 精确匹配 |
| 留存策略 | 分层留存（永久/压缩/归档） | ttl 到期清除 | 分层留存（永久/压缩/归档） |
| 注入方式 | 上下文管理器自决 | 态控调度器路由 + 上下文管理器注入字段 | 上下文管理器自决 |
| changeLevel 判定 | 核心实体增删→major，微调→minor，撤销→invalid | 无（进度不是变更） | 字段结构变更→major，值微调→minor，改回原值→invalid |

### 3.9 统一环节操作模型——五步流水线（v4.3 新增）

态控架构的两种任务模式（field_based / topic_based）遵循相同的五步操作流水线。差异只在具体实现参数中——不是两套系统，是同一套系统的两种环节配置。

```
┌─────────────────────────────────────────────────────┐
│  步骤一：状态机路由                                    │
│  ┌─────────────────────────────────────────────────┐│
│  │ 用户输入到达 ANALYZING                           ││
│  │   ├─ 元指令匹配（唤醒/退出/取消）→ 直接处理         ││
│  │   ├─ sessionCheckpoint 恢复检查（field_based 专属） ││
│  │   ├─ topicIndex 语义检索（topic_based 专属）        ││
│  │   └─ 意图识别 → 确定目标环节                       ││
│  │ 前置依赖校验 → 通过? → 路由到 IN_SESSION(目标环节)  ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  步骤二：环节上下文加载                                │
│  ┌─────────────────────────────────────────────────┐│
│  │ 从三个存储来源加载环节上下文:                       ││
│  │   ├─ roomConversationLog(roomId) 最近N轮对话      ││
│  │   ├─ sessionCheckpoints.stepSnapshots[环节]       ││
│  │   └─ contractStore.domainRules(stepName) 已生效规则 ││
│  │ topic_based 额外加载: topicEvolution + topicIndex  ││
│  │ field_based: stepName DET 精确匹配                 ││
│  │ topic_based: topicId embedding 语义匹配            ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  步骤三：内容拼接（环节自治）                           │
│  ┌─────────────────────────────────────────────────┐│
│  │ 每个环节的【上下文管理器】决定拼接规则:             ││
│  │   ├─ 对话窗口: 取最近N轮（默认20轮）                  ││
│  │   ├─ 结构化字段: 注入collectedFields（仅field_based）││
│  │   ├─ 规则注入: 按immutableLevel阈值筛选domainRules  ││
│  │   └─ 搜索片段: 用户引用历史时追加匹配片段            ││
│  │ 上下文管理器是独立状态机——只管拼接，不执行环节宪法    ││
│  │ 态控调度器不干预拼接                                 ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  步骤四：模型计算                                     │
│  ┌─────────────────────────────────────────────────┐│
│  │ 模型无状态——它只看到拼接好的上下文 + 用户最新输入    ││
│  │ 在局部宪法约束下自主完成:                          ││
│  │   ├─ 多轮追问                                     ││
│  │   ├─ 字段/值域校验（field_based: DET辅助）         ││
│  │   ├─ 语义边界判断（topic_based: embedding辅助）    ││
│  │   └─ 收敛检测 → turnType=complete               ││
│  │ 出口: 结构化JSON（contract-out schema）           ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  步骤五：记录落盘                                     │
│  ┌─────────────────────────────────────────────────┐│
│  │ 实时记录: 每turn → roomConversationLog追加一行    ││
│  │ 收敛记录: complete → sessionCheckpoint更新快照    ││
│  │ 变更记录: changeLevel event → contractStore       ││
│  │ 规则提炼: ruleMiningQueue积压 → N10异步提炼        ││
│  │ 上下文清空: 物理清除LLM上下文，仅结构化数据持久化     ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

**五步流水线中的两种环节配置**：

| 步骤 | field_based 配置 | topic_based 配置 |
|------|-----------------|-----------------|
| 路由 | stepName 精确匹配，用户说"去N1"即切 | topicId embedding 检索，"上次那个写诗的项目"需语义匹配 |
| 切换确认 | 进度保护——有未提交字段时弹"保存后切换?" | 意图确认——embedding匹配到多个topicId时弹"是哪一个?" |
| 上下文 | collectedFields + roomConversationLog(roomId) | topicSnapshot + topicEvolution events + roomConversationLog(roomId) |
| 校验 | DET字段白名单 + 值域硬校验 | LLM语义边界 + DET confidence软校验 |
| 出口 | JSON Schema硬卡死，字段齐全=完成 | event类型判断，changeLevel分层留存 |
| 记忆 | sessionCheckpoint + roomConversationLog | topicEvolution + roomConversationLog |

**核心纪律**：
- 态控调度器只管步骤一（路由）和步骤五（记录），不干预步骤二三四
- 上下文管理器管步骤三（拼接）——独立状态机，不执行环节宪法，只管"怎么从态控调度器交给它的内容中组装上下文"
- 环节宪法管步骤四（模型行为规则）
- 五步流水线的**接口统一**：两种 taskType 在外部接口上完全相同（路由信号→上下文加载→拼接输出→模型计算→落盘确认）。**内部实现分支**：步骤一匹配模式（DET vs embedding）、步骤二加载源（sessionCheckpoint vs topicEvolution）、步骤三锚点模式（字段硬门控 vs 三层注入+两阶段检索）、步骤四校验方式（值域 vs 格式）、步骤五落盘目标（sessionCheckpoint vs topicEvolution event）各有差异

### 3.10 @importance 重要性分层机制

与 `immutableLevel`（权限轴：谁能改）正交的第二轴——重要程度轴（哪个更关键）。房间多、规则密，需要一套标记告诉上下文管理器"有限的 token 预算优先分给谁"。

**三层结构，独立计数：**

| 层级 | 标记位置 | 说明 | 示例 |
|------|---------|------|------|
| **房间级** | 环节宪法头部 `<!-- @importance -->` | 同一 taskType 下所有环节中，哪个最重要 | field_based 16 个环节中 N1 为 critical；topic_based 全屋定制中设计环节为 critical |
| **产出物级** | 环节宪法 `<!-- @importance:deliverable -->` | 同一环节的 machineReadable 中哪个产出物是核心 | N1 的 doList 为 critical；设计方案的厨房设计为 critical |
| **规则级** | 领域规则节点 `ruleNode.importance` | L0/L1/L2/L3 每层独立，各层内最多 1 条 critical | L0 安全红线为 critical；L1 某条基线规则为 critical |

**枚举值及约束：**

| 值 | 含义 | 每层上限 | 作用 |
|------|------|---------|------|
| critical | 极其重要——此层内最关键 | 1（必须用户确认） | roomConversationLog 保留 10 轮；规则排 prompt 头部；UI 高亮 |
| high | 高重要 | ≤ ⌊该层总数/3⌋，可为 0 | roomConversationLog 保留 6 轮；规则优先注入 |
| normal | 普通（默认） | 无限制 | roomConversationLog 保留 3 轮；正常排序 |
| low | 低重要 | 无限制 | roomConversationLog 保留 1 轮；可跳过注入 |

**消费方：上下文管理器拼接策略**

buildPromptContext() 在组装 token 预算时按 importance 分级：

1. **roomConversationLog 截断**：汇总全部环节的历史记录时，按环节 `@importance` 分配保留轮次——critical=10/high=6/normal=3/low=1。同是 4096 token 窗口，N1(critical) 的记录占大头，N15(normal) 占小头。

2. **规则注入排序**：模型注意力在 prompt 头部衰减最慢。规则注入按 `ruleNode.importance` 排序——critical→high→normal→low。同 importance 内按 strengthens 权重。

3. **冲突仲裁**：两条同 immutableLevel 的规则冲突时，importance 高的直接胜出，不触发 LLM 调用。**跨层冲突时（如 L0 vs L2）按 immutableLevel 直接裁决，importance 不参与跨层仲裁——详见 §3.7.2 跨层仲裁的边界声明（v4.6 新增）**。

约束纪律：high 是稀缺标签——不能随手标。极端合法配置：每层 1 critical + 0 high + 其余全 normal。

**防偷懒规则——每层必须恰好 1 条 critical 或显式声明 allow_no_critical：**

| 层级 | 是否允许 0 critical | 说明 |
|------|-------------------|------|
| 房间级 | ❌ 必须恰好 1 条 | 同一 taskType 下必须有一个核心环节——用户确认 |
| 产出物级 | ❌ 必须恰好 1 条 | 同一环节的 machineReadable 中必须有一个核心产出物——用户确认 |
| 规则级 L0 | ❌ 必须恰好 1 条 | 根宪法 8 条中安全红线即为 critical——设计者确认 |
| 规则级 L1 | ❌ 必须恰好 1 条 | 基线规则中必须有一个命门——设计者确认 |
| 规则级 L2 | ⚠️ 可 `allow_no_critical: true` | 用户确认规则中可能没有"唯一最关键"的偏好——在环节宪法 `@outputs` 旁标注理由 |
| 规则级 L3 | ⚠️ 可 `allow_no_critical: true` | 模型推断规则天然没有"唯一最关键"——在环节宪法 `@outputs` 旁标注理由 |

`allow_no_critical` 由 N2 覆盖率校验时审查。申请时在宪法头部 `<!-- @importance -->` 旁标注：`<!-- @importance:allow_no_critical reason="L2层均为平权用户偏好，无唯一命门" -->`。不允许"0 critical + N high"绕过——要么恰好 1 critical，要么显式声明 0 critical + 理由。

## 四、节点体系

### 4.1 节点意图收敛链

每个节点将弥散输入收敛到当前目标所要求的结构化输出。在环节级自治（环节级自治）范式下，收敛链（收敛链）的"环节内"部分由模型实例自主完成：

| 节点 | 输入（弥散态） | 收敛目标（contract-out（输出契约）） | 执行体 | taskType 影响 |
|------|--------------|----------------------------------|--------|-------------|
| **ANALYZING（分析态）** | 用户口语"柜体深度改一下" | `{choice（选项字母如A/B/C/D/E）, logprobs（API返回的真实概率分布）, intent（从choice映射的意图名）, inputNature（v5.1新增：S/T/U）, extracted（已提取信息）, topicId（topic_based时自动生成）}`——v5.1扩展：双维度分类，强制选择+API logprobs替代LLM自由生成confidence。intent由DET从choice映射（如choice=A→intent="resize"） | LLM | choice 由模型从有限选项中选择；logprobs 由 API 返回；inputNature 并行分类；DET 读取 logprobs 交叉裁决路由 |
| IN_SESSION(field_based) | intent+口语+参数树（环节内多轮） | `{turnType, askingField=字段名, message, collectedFields, result, changeLevel, changeLevelReason}` | LLM（环节内自治） | askingField 为预定义字段名；每 turn 实时写入 roomConversationLog；complete 时更新 sessionCheckpoint 和 contractEvolution |
| IN_SESSION(topic_based) | intent+口语+主题边界+topicId+topicEvolution 最新 stateSnapshot（环节内多轮） | `{turnType, askingField=主题标签, message, collectedFields, result, changeLevel, changeLevelReason}` | LLM（环节内自治） | askingField 为动态主题标签；每 turn 实时写入 roomConversationLog；exit 时追加 event 到 topicEvolution；模型可 infer 新规则（model_inferred）→ ruleCandidates |
| DET 复验 (field_based) | result+constraints（complete 时） | `{valid:true}/{valid:false, reason}` | DET | 值域校验 + contractEvolution 检测（changeLevel 事件分层留存） |
| DET 复验 (topic_based) | result+format-constraints（complete 时） | `{valid:true, reason}` | DET | 格式校验 + topicEvolution event 追加 |
| PREVIEWING（预览态）（可选子环节） | fieldPath（字段路径）+old（旧值）/new（新值）+modelId（模型标识） | `{previewUrl（预览地址）, diffSummary（差异摘要）}` | DET（可选） | 通常仅 field_based（基于字段） 启用 |
| WAITING_CONFIRM（等待确认） | previewUrl（预览地址）+diffSummary（差异摘要） | `{decision（决策）:"confirm（确认）"/"reject（拒绝）"}` | 人 | — |
| EXECUTING（执行态） | fieldPath（字段路径）+newVal（新值）+modelId（模型标识） / result+outputTarget（输出目标） | `{status（状态）:"success"}` | DET | 确定性收敛；topic_based 任务在 EXECUTING 阶段将输出写入文件后，追加 event 到 topicEvolution |

**turnType 与 changeLevel 的区别**：
- `turnType`：轮次类型（ask/reply/complete/validation_failed/off-task/giveup），描述当前轮的交互状态，每 turn 都输出
- `changeLevel`：变更级别（major/minor/invalid），描述本轮变更相对上一版的信息净价值，仅在内容发生变更时输出
- 两者独立判定：turnType=complete 时 changeLevel 可能为 major（结构性变更后完成），也可能为 minor（微调后完成）

注：PREVIEWING（预览态） 为 IN_SESSION（会话中） 的可选子环节，在需要可视化 diff（差异） 的不可逆操作场景下启用，通常仅 field_based（基于字段） 任务需要（如改柜体参数前预览 diff（差异）），topic_based（基于主题） 任务通常不需要（如写诗不需要预览 diff（差异））。

### 4.2 节点状态

#### 4.2.1 标准状态集（环节级自治方式）

| 状态 | 类型 | 含义 | 默认执行体 | 默认模型（LLM节点） | 备注 |
|------|------|------|-----------|-------------------|------|
| IDLE（空闲态） | 稳态 | 空闲，等待唤醒 | — | — | 无人/无程序 |
| LISTENING（监听态） | 稳态 | 待命，等待用户输入 | 人 | — | 唤醒后进入 |
| ANALYZING（分析态） | 瞬态 | 意图识别环节（v5.0 重新定义） | LLM | 通用轻量模型 | 有限展开后收敛到 intent（意图）。ANALYZING 是环节，受根宪法第1条（环节隔离）约束——只能做意图识别，不能采字段、不能追问、不能给最终回复。topic_based 时自动生成 topicId 并初始化 topicEvolution。**v4.2：topic_based 任务额外执行规则问询（user_initiated 来源）** |
| IN_SESSION（会话中） | 稳态 | 环节执行中 | LLM | 通用模型（状态级默认）/ 专用模型（意图级绑定） | 模型实例在环节内自主对话，调度器等待返回。子状态通过模型返回的 turnType（轮次类型） 可观测：ask（追问）/reply（回复）=追问中，validation_failed（校验失败）=校验失败追问中，off-task（偏离任务）=偏离处理中，complete=待 DET 复验。PREVIEWING（预览态）（可视化 diff（差异））作为 IN_SESSION 的可选子环节，在涉及不可逆操作的场景启用。taskType（field_based（基于字段）/topic_based（基于主题））在意图识别阶段确定（引用 v5.3 节点说明的 N1/N3），影响 askingField（追问字段） 语义和 context-manager（上下文管理器） 锚点模式。topic_based 任务在节点 exit 时调度器自动追加 event 到 topicEvolution |
| CLARIFYING（澄清态） | 守护态 | 校验失败追问转发 | DET（转发） | — | 调度器复用模型 message 转发用户（不调 LLM）。此状态为监控标记态，便于运维监控校验失败率和触发频率 |
| WAITING_CONFIRM（等待确认） | 守护态 | 等待用户确认 | 人 | — | 责任节点，不可逆决策 |
| EXECUTING（执行态） | 瞬态 | 执行操作 | DET | — | 确定性收敛。执行前先过 DET 值域复验。topic_based 任务在 EXECUTING 阶段将输出写入文件后，调度器自动追加 event 到 topicEvolution，stateSnapshot 标记为 checkpoint（如 turnType 为 complete）或 active（如为中间步骤） |
| **SLACK_NODE（松弛节点）** | **稳态** | **仅根宪法约束的闲聊** | **LLM** | **通用轻量模型** | **v5.1 纳入标准状态集。用户闲聊、发散、跳跃话题。输出自然语言文本，不进入下游确定性节点。受根宪法4条约束（含环节隔离——但 SLACK_NODE 的"本环节任务"定义为"自然对话，无需收敛"）。"切断房间"→ 回 LISTENING。详见 §4.2.2** |
| **TEMP_TOPIC（临时主题）** | **稳态** | **运行时创建的仅根宪法房间** | **LLM** | **通用模型** | **v5.1 新增。ANALYZING 返回 intent=other + inputNature=T 时，用户命名后动态创建。仅受根宪法4条约束，无环节管道。一旦创建即纳入房间体系：roomId、keywords、roomConversationLog、上下文管理器、roomStateIndex。"切断房间"切换。用户可触发总结产出。N12 远期可检测聚集建议升级为正式房间。详见 §4.2.2** |
| CLOSING（关闭态） | 瞬态 | 退出 | DET | — | 清理资源 |

#### 4.2.2 节点类型体系（v5.1 扩展：三级约束梯度）

态控有三类节点，分别对应不同的约束强度和收敛义务：

| 节点类型 | 约束强度 | 宪法约束 | contract-out 要求 | 是否参与收敛链 | 切换 |
|---------|---------|---------|-------------------|-------------|------|
| 收敛节点 | 高 | 环节宪法 + 根宪法 | 结构化 JSON（field_based）或 结构化 JSON + topicEvolution event 追加（topic_based） | 是 | "切断房间" |
| **TEMP_TOPIC（临时主题）** | **中** | **仅根宪法 4 条** | **用户触发总结时生成结构化产出** | **否（单态，无环节管道）** | **"切断房间"** |
| 松弛节点（SLACK_NODE） | 低 | 仅根宪法 4 条 | 自然语言文本 | 否（暂停态） | "切断房间" → LISTENING |

三者统一用 M1 元指令"切断房间"退出。约束轻重只决定**房间里有什么**（宪法层级、产出能力、存储），不改变退出方式。

**TEMP_TOPIC（v5.1 新增）**：运行时动态创建的房间——不是 N1 预定义的正式房间，也不是无锚点的 SLACK_NODE。一旦用户命名（如"新能源趋势分析"）并确认关键词，即纳入 roomStateIndex，与其他房间并列。特性：

- **宪法**：仅根宪法4条，无环节宪法、无领域规则
- **环节管道**：无（单态，不经过 ANALYZING→IN_SESSION→EXECUTING 管道）
- **上下文管理**：上下文管理器拼接 conversationLog（与其他房间同一机制）
- **切换**："切断房间" + S3 物理释放，切换后可从 roomStateIndex 返回
- **产出**：用户触发"帮我总结"时生成结构化产出，存入 contractStore
- **升级路径**：N12 规则提炼远期可检测 temp topic 聚集模式 → 建议升级为正式房间

**TEMP_TOPIC 创建流程**：
```
ANALYZING: intent=other + inputNature=T
  → 调度器："这是一个新话题，起个名字方便以后切换。叫什么？"
  → 用户起名 + 确认关键词
  → 调度器创建 room:
     { roomId, roomName, keywords: [...], constitution: root_only, stages: none }
  → 写入 roomStateIndex
  → 进入 TEMP_TOPIC 单态会话
```

**松弛节点（SLACK_NODE，v5.1 纳入统一体系）**：合法存在的低约束节点，仅受根宪法约束。用户在松弛节点中可以闲聊、发散、跳跃话题。松弛节点的输出不进入下游确定性节点——它是一段"暂停"，不是管道的一部分。**v5.1："切断房间" → 回 LISTENING，与其他房间统一用 M1 元指令退出。**

松弛节点的存在使智能体能够支持"闲聊→任务"的自然过渡（如用户在闲聊中突然说"算了不聊柜子了，帮我写首诗"）。但松弛节点的边界本身需要经过边界紧张度测试——如果闲聊落出率高，说明松弛节点的边界划定有问题，需要回溯第一步重审边界。

松弛节点不区分 taskType——松弛节点本身就是"暂停"，不参与收敛。TEMP_TOPIC 同样不区分 taskType——TEMP_TOPIC 本身就是没有环节管道的单态房间。

#### 4.2.3 节点类型动态转换规则（v5.1 扩展）

| 转换方向 | 触发条件 | 动作 | 守卫规则 |
|---------|---------|------|---------|
| 松弛节点 → 收敛节点 | 用户输入明确任务指令 | 意图识别接管，进入收敛链 | 架构机制 M1 优先匹配；logprobs probability≥0.7 方可转换 |
| 收敛节点 → 松弛节点 | 用户放弃或主动脱离 | contract-out 标记为 abandoned，回退至 IDLE 或进入松弛节点 | 架构机制 M1 定义放弃指令匹配模式，不接受环节宪法覆盖 |
| 收敛节点 → 另一收敛节点 | 用户发出元指令 | 物理隔离当前上下文 Γ，动态实例化新节点 | 元指令集由架构机制 M1 定义，不接受动态修改 |
| **TEMP_TOPIC → 收敛节点** | **用户输入匹配已知 intent** | **ANALYZING 重新识别，进入对应正式房间** | **logprobs probability≥0.7** |
| **收敛节点 → TEMP_TOPIC** | **ANALYZING: intent=other + inputNature=T** | **用户命名后创建 TEMP_TOPIC 房间** | **用户确认房间名和关键词** |
| **SLACK_NODE → LISTENING** | **"切断房间"** | **M1 口令精确匹配 → S3 物理释放 → 回 LISTENING** | **DET EXACT_MATCH** |
| **TEMP_TOPIC → LISTENING** | **"切断房间"** | **M1 口令精确匹配 → S3 物理释放 → 归档 → 回 LISTENING** | **DET EXACT_MATCH** |

taskType（任务类型） 转换：taskType 在意图识别阶段确定后（引用 v5.3 节点说明的 N1/N3），运行时不变。如果用户在 field_based（基于字段） 任务中突然切换到 topic_based（基于主题） 任务（如记账中突然说"帮我写首诗"），这通过元指令识别机制触发节点切换——物理隔离当前上下文，进入新的 IN_SESSION（会话中）（topic_based（基于主题））。taskType 不在环节内动态切换。

#### 4.2.4 节点模型选择

第一层（状态级默认模型）：每个 LLM 节点有预设的默认模型实例，不依赖意图。ANALYZING（分析态） → 通用轻量模型（意图识别不需要深度能力）；IN_SESSION（会话中） → 通用模型。此层解决"ANALYZING（分析态） 节点本身用哪个模型"的循环依赖。

第二层（意图级绑定）：当节点产出意图后，下游节点根据路由表中的"意图-模型绑定"覆盖默认模型。IN_SESSION（会话中） + intent（意图）="write_poem（写诗）" → 专用文学模型。详见 §5 路由表设计。

taskType 对模型选择的影响：topic_based（基于主题） 任务倾向使用深度模型（主题理解和创意生成需要深度能力），field_based（基于字段） 任务通用模型即可（字段映射不需要深度能力）。路由表第二层绑定时，topic_based（基于主题） intent 可绑定专用深度模型，field_based（基于字段） intent 用默认通用模型。


## 五、路由表

路由表是确定性代码，不调 LLM。路由规则必须在设计时显式定义。路由表同时承担两个职责：状态路由（决定下一个状态）和模型选择（决定 LLM 槽位由哪个模型实例填充）。

两层模型选择机制：
第一层（状态级默认模型）：每个 LLM 节点有预设的默认模型实例，不依赖意图。此默认值定义在节点定义中（见 §4.2.1 标准状态集的"默认模型"列）。
第二层（意图级绑定）：当上游节点产出 intent（意图） 后，下游节点可根据"状态+intent（意图）"组合覆盖默认模型。此绑定配置在路由表中。

**路由表示例：**

| 当前状态 | contract-out（输出契约） 关键字段 | 下一状态 | 路由规则 | 模型覆盖（第二层） |
|---------|--------------------------------|---------|---------|-----------------|
| ANALYZING（分析态） | intent="resize（调整尺寸）" (field_based) | IN_SESSION | 硬匹配 | —（IN_SESSION 用默认通用模型） |
| ANALYZING（分析态） | intent="write_poem（写诗）" (topic_based) | IN_SESSION（含 CONSTITUTION_GEN 例外路径） | 硬匹配 | 专用文学模型（topic_based 需深度能力） |
| ANALYZING（分析态） | intent="roleplay（角色扮演）" (topic_based) | IN_SESSION | 硬匹配 | 专用角色扮演模型 |
| ANALYZING（分析态） | intent="exit（退出）" | CLOSING（关闭态） | 硬匹配 | — |
| **ANALYZING** | **intent=other + inputNature=S（闲聊）** | **SLACK_NODE** | **硬匹配** | **通用轻量模型** |
| **ANALYZING** | **intent=other + inputNature=T（任务导向）** | **TEMP_TOPIC** | **硬匹配（先经用户确认命名）** | **通用模型** |
| **SLACK_NODE** | **"切断房间"（M1 口令）** | **LISTENING** | **DET EXACT_MATCH** | **—** |
| **TEMP_TOPIC** | **"切断房间"（M1 口令）** | **LISTENING** | **DET EXACT_MATCH** | **—** |
| **TEMP_TOPIC** | **用户输入匹配已知 intent** | **ANALYZING** | **硬匹配** | **—** |
| IN_SESSION | turnType（轮次类型）="complete" + intent="resize" | EXECUTING（执行态）（经 DET 值域复验） | 硬匹配 | — |
| IN_SESSION | turnType="complete" + intent="write_poem" | EXECUTING（执行态）（经 DET 格式校验） | 硬匹配 | 专用文学模型 |
| IN_SESSION | turnType="validation_failed" | CLARIFYING（澄清态） | 硬匹配 | — |
| IN_SESSION | turnType（轮次类型）="off-task" | ANALYZING（分析态） | 硬匹配 | — |
| IN_SESSION | turnType="giveup" | LISTENING（监听态） | 硬匹配 | — |
| IN_SESSION | turnType="ask"/"reply" | IN_SESSION（继续） | 硬匹配 | — |
| CLARIFYING（澄清态） | — | IN_SESSION | 硬匹配 | — |
| EXECUTING（执行态） | status="success" | LISTENING（监听态） | 硬匹配 | — |

ANALYZING（分析态） 节点本身使用其状态级默认模型（通用轻量模型），不依赖第二层绑定，循环依赖打断。


## 六、容错与纠错机制

### 6.1 软约束与硬降级链

#### 6.1.1 硬卡机制

contract-out schema（输出契约模式） 是硬边界，格式错误或违反宪法直接退回上游重试。

#### 6.1.2 降级机制

重试上限触发后，强制降级路由至 CLARIFYING（澄清态） 或 WAITING_CONFIRM（等待确认）。

#### 6.1.3 四项降级链（通用底线，不可缩减）

L1 结构校验 + DET 值域复验 + logprobs 检查 + 硬编码兜底。field_based 和 topic_based 的差异仅在 DET 值域复验（值域校验 vs 格式校验）。

**v5.5 修正：移除"跨任务延伸检测"**——跨任务是用户显式确认的行为（用户说"切断房间"或 off-task→ANALYZING重新识别→用户确认），不需要架构层自动化检测。降级链从五项精简为四项。

#### 6.1.4 增强降级链（高风险场景扩展）

对于医疗预问诊、复杂客服、金融风控等高危场景，在四项降级链之后可追加 human_takeover（人工接管） 扩展层（simple（简单）→escalated（升级）→human_takeover（人工接管），含 SLA（服务等级协议））。SLA 超时未响应时按 preset（预设） 降级到 hardcoded（硬编码） 或 abort（中止）。详见附录 A。四项降级链是通用底线，human_takeover（人工接管） 是高风险场景的扩展，不可替换四项中的任何一项。

#### 6.1.5 降级链检查 + 硬编码兜底

| 项 | 执行体 | 时机 | 对应约束 | 说明 |
|----|--------|------|-------------|------|
| L1（一级） 结构校验 | DET | complete（完成） 时 | 根宪法第2条（输出格式） | JSON.parse（JSON解析） + Schema（模式） 校验，失败→重试1次→熔断 |
| DET 值域复验 | DET | complete 时 | — | field_based（基于字段） 做值域校验（金额上限、时间格式、必填字段…），topic_based（基于主题） 做输出格式校验（JSON Schema / 文本格式 / 字数 / 结构…）。规则来源：环节宪法 @section validation 的结构化 JSON） |
| logprobs 检查（v5.1 修正：替代 confidence 检查） | DET | complete 时 | — | v5.1：ANALYZING 用强制选择+logprobs，DET 读取 logprobs 转换为 probability 做阈值裁决（probability < threshold → intent=other）。IN_SESSION 的"硬撑式确认"检测保留 pattern 扫描词表。冷启动期间采用软拦截——标记 warning 附加到返回中，不直接拦截；冷启动结束后恢复硬拦截。原 confidence 检查因 LLM 自由生成 confidence 不可靠（幻觉式自信）已废弃 |
| 硬编码兜底 | DET | 熔断时 | — | 所有校验失败后的最终兜底，硬编码回复 |

> **v5.5 修正：移除"跨任务延伸检测"**——跨任务是用户显式确认的行为（M1 switch 口令"切断房间"或 off-task→ANALYZING重新识别→用户确认），不需要降级链层的自动化检测。降级链从五项精简为四项。
| 硬编码兜底 | DET | 熔断时 | — | 所有校验失败后的最终兜底，硬编码回复 |

**v4.2：规则冲突时的询问逻辑作为扩展守卫**——当匹配到的领域规则中存在 conflicts_with 关系的节点时，触发询问让用户决策。此守卫不属于四项检查，是运行时决策辅助，按 immutableLevel 决定询问策略（L0 直接覆盖不询问，L1 推荐询问，L2 偏好询问）。

### 6.2 用户纠错的状态路由

状态机维护一个持久化的 contractStore（契约存储），保存每个节点完成时的 contract-in（输入契约） 和 contract-out（输出契约） 副本。contractStore（契约存储）独立于执行体实例的生命周期——Agent（智能体） 消亡后，其产出的契约副本仍可被后续节点或回滚流程访问。

参数级纠错：路由至 CLARIFYING（澄清态），提取新参数，重新进入执行管道。CLARIFYING（澄清态） 复用模型 message 转发用户（不调 LLM）。field_based（基于字段） 任务的参数级纠错是字段值修改，topic_based（基于主题） 任务的参数级纠错是主题方向微调。

意图级纠错（逃生舱）：若用户反馈表明当前意图完全错误，状态机支持强制回滚至 ANALYZING（分析态） 节点重新分类。回滚数据流如下：

状态机从 contractStore（契约存储） 取出 ANALYZING（分析态） 节点的原始输入副本（ANALYZING 的输入是用户口语，非结构化 contract-in，存储的是原始用户输入文本）

重新实例化 ANALYZING（分析态） 节点（注入原始输入 + 宪法）

重新执行意图识别，产生新的 intent（可能伴随 taskType 变化）

清空当前 activeSession，按新 intent 和新 taskType 重新分发

环节内偏离处理：当模型返回 turnType（轮次类型）="off-task（偏离任务）" 时，调度器暂存 collectedFields（已采集字段） 到 _pendingCollectedFields（待定已采集字段）（默认 24h，可配置，存储位置后续章节定义），将 offTaskInput（偏离任务输入） 送入 ANALYZING（分析态） 重新意图识别。下次同一用户进入同一环节时，调度器询问是否继续上次未完成的记录。field_based（基于字段） 任务的 collectedFields（已采集字段） 是已采集的字段，topic_based（基于主题） 任务的 collectedFields（已采集字段） 是已讨论的主题摘要。

用户明确离开：直接作废，清空 activeSession（活跃会话）。不留草稿，不保存未完成记录，不恢复现场。_lastRecord（最后记录）不清空（用于"记错了"场景，后续章节定义）。

用户放弃：模型返回 turnType（轮次类型）="giveup（放弃）" → 调度器清空 activeSession（活跃会话） → LISTENING（监听态）。

### 6.3 系统崩溃恢复快照

非正常退出（系统崩溃、断电等）时，依赖 sessionCheckpoint 的自动快照恢复——activeSession 的 collectedFields 和当前环节名自动落盘，下次启动恢复现场。此为独立的容错机制，与元指令切换无关——元指令是用户主动行为，不需要快照保护。
## 七、约束体系（v5.0 重写）

### 7.1 三层宪法体系总览

v5.0 核心变更：按约束对象分层，每一层的约束对象唯一。取消 v4.7 的"公共规则"中间层，内容分拆到根宪法/环节宪法/架构机制。

| 层级 | 名称 | 约束对象 | 是否可修改 | 说明 |
|------|------|---------|-----------|------|
| 第0层 | 根宪法 | **所有环节模型**（ANALYZING + IN_SESSION） | 设计者写死，不可运行时修改 | 不可逾越的系统红线 |
| 第1层 | 环节宪法 | **特定环节模型** | 设计者静态编写（默认）；开放型环节可运行时动态生成 | 环节内模型的行为规则 |
| 第2层 | 领域规则 | 特定主题类型 | 用户确认后生效，可随时修改或废弃 | L0-L3 四级权限梯度 |
| — | **架构机制** | **调度器/DET** | 设计者定义 | 元指令集、降级链、仲裁权、节点转换守卫——约束调度器和DET行为，非模型约束 |

**v4.7→v5.0 公共规则拆解**：

| v4.7 公共规则 | v5.0 归属 | 理由 |
|------|------|------|
| 规则1：输出纯 JSON | 根宪法第2条（输出格式） | 这是所有环节模型都必须遵守的格式约束 |
| 规则2：坚持真实判断 | 根宪法第3条（收敛义务+坚持真实判断） | 不迎合用户是所有环节模型的义务 |
| 规则3（一次聚焦，v5.0 取消公共规则）→ 根宪法第1条（环节隔离）天然覆盖 | — |
| 追问模板（原 7.3 追问部分） | 环节宪法 @section ask-rules | 追问是环节内行为，模板随环节而异 |

### 7.2 根宪法（4 条，约束所有环节模型）

**定位**：所有环节模型（ANALYZING + IN_SESSION）都必须遵守的底线条款。由设计者写死，不可运行时修改，不可被环节宪法覆盖。

| 编号 | 条款 | 内容 | 可被环节宪法覆盖 |
|:--:|------|------|:---:|
| 1 | **环节隔离** | 只能做本环节任务，非本环节任务返回 off-task（偏离）。本环节任务由环节宪法第一条定义。ANALYZING（分析态）只能做意图识别。IN_SESSION（会话中）只能做本环节任务。 | ❌ 不接受覆盖 |
| 2 | **输出格式** | 模型实例返回调度器的结果必须是符合 turnType schema 的结构化 JSON（含 askingField 双模式语义 + changeLevel 预判字段 + changeLevelReason 理由字段，两种 taskType 模式均适用） | ❌ 不接受覆盖 |
| 3 | **收敛义务 + 坚持真实判断** | 所有 LLM 节点必须在当前环节内收敛，输出结构化 JSON。不伪造数据、不假装理解、不迎合用户、不无中生有。确认无误就坚持，确实错了就纠正。 | ❌ 不接受覆盖 |
| 4 | **不编造功能** | 所有 LLM 节点不得生成用户未交代、系统不具备的功能或信息。这是安全红线的真核心——不是场景特定的"禁止恶意内容"，而是全局硬约束"不许无中生有"。 | ❌ 不接受覆盖 |

**ANALYZING（分析态）的环节隔离特化**（v5.0 新增，v5.1 修正）：ANALYZING 的"本环节任务"定义为"意图映射"——将用户输入映射到已知 intent。v5.1 修正：ANALYZING 用强制选择+API logprobs（非 LLM 自由生成 confidence）。如果 logprobs probability 不足以确认任何已知 intent，返回 `intent=other`（低 probability，调度器按 probability 路由）。注意：ANALYZING 只产出 intent，不产出 turnType——turnType 是 IN_SESSION 的输出格式。`turnType=off-task` 是 IN_SESSION 内模型检测到偏离时的输出，不是 ANALYZING 的输出。ANALYZING 不得采集字段、不得追问、不得给出最终回复。

### 7.3 架构机制（约束调度器/DET，非模型约束）

以下四条从 v4.7 根宪法移出，属于架构规范，约束调度器和 DET 行为：

| 编号 | 机制 | 约束对象 | 内容 |
|:--:|------|:---:|------|
| M1 | **元指令集（v5.3：S3 + roomStateIndex）** | 调度器（第0层）+ 内存管理模块 + context-manager | 口令词表。四类通用口令：wake/switch/exit/cancel。口令层 EXACT_MATCH→直切（0 次 LLM）。**v5.3 强化**：wake/switch/exit 触发 S3 物理 KV Cache 释放 + roomStateIndex 注入——wake=创建新推理会话 + 从 contractStore 恢复 checkpoint + `buildRoomIndex()` 注入 ANALYZING 以获取全局房间状态；switch=释放当前房间 KV Cache + 归档 + 创建新会话 + 恢复目标房间 checkpoint + `buildRoomIndex()` 注入；exit=释放 KV Cache + 归档回 IDLE。roomStateIndex 提供全窗口房间摘要（各房间待完成/已完成数），使 ANALYZING 可针对性建议（如"您记账房间还有一条未完成"）。设计者定义，DET+内存管理+context-manager 执行 |
| M2 | **降级链四项检查（v5.5 修正）** | 调度器/DET | L1 结构校验（DET）+ DET 值域复验（DET，field_based值域/topic_based格式）+ logprobs 检查（DET）+ 硬编码兜底（DET）。均不可缩减。**v5.5 移除跨任务延伸检测——跨任务是用户显式确认行为，不需要降级链层自动化检测** |
| M3 | **仲裁权 DET 优先** | 调度器 | 调度器 DET 校验结果优先于模型内部校验结果。两者冲突时以调度器 DET 为准 |
| M4 | **节点转换守卫** | 调度器 | 定义环节间转换的触发条件和守卫规则。IN_SESSION 期间用户输入偏离时，调度器 DET 关键词扫描可辅助触发偏离标记，但最终判断由模型给出 |

### 7.4 环节宪法（约束特定环节模型）

（结构模板与 v4.7 一致，v5.0 在第一条强化）

**v5.0 变化**：

1. **第一条强制为首要内容**：环节宪法第一条必须定义"本环节任务是什么"并强调"非本环节任务返回 off-task"。

2. **追问模板下放到 @section ask-rules**：原来在公共规则里的追问模板（追问类型/主题/格式三维度），现在由各环节宪法的 @section ask-rules 自行定义。

3. **环节宪法结构模板**（与 v4.7 相同，18 标签 @section）：
   ```
   <!-- @constitution <session_name> -->
   <!-- @section taskType -->
   <!-- @section role -->（第一条：本环节任务定义 + off-task 声明）
   <!-- @section convergence -->
   <!-- @section field-rules | topic-boundary -->
   <!-- @section parsing -->
   <!-- @section validation -->
   <!-- @section ask-rules -->（含追问模板，v5.0 从公共规则下放）
   <!-- @section completion -->
   <!-- @section modification -->
   <!-- @section off-task-detection -->
   <!-- @section giveup -->
   <!-- @section output-schema -->
   <!-- @section tunable -->
   <!-- @section domain-rule-priority -->
   <!-- @importance -->
   <!-- @outputs -->
   ```

### 7.5 宪法仲裁流程（v5.0 适配三层）

```
环节宪法条款与根宪法冲突
    ↓
反对称仲裁（根宪法无条件优先）
    ↓
冲突条款以根宪法为准，环节宪法对应条款标记为"不可用"
```

因公共规则已取消，仲裁流程从四层缩减为三层。

---



## 八、契约体系

### 8.1 双层契约

Contract-in（输入契约）：为当前节点提供最小充分条件集，屏蔽无关历史。

Contract-out（输出契约）：硬性 Schema（模式） 约束，强制模型输出结构化数据，阻断发散。

契约的模态无关性：Contract（契约） 定义的是数据格式、值域、可选性和副作用标记，不定义产生该数据的执行体类型或模态。多模态模型和单模态模型可以在同一个状态机中共存，状态机只需验证 contract-out（输出契约） 是否符合 schema（模式），无需知道上游的模态。

契约的 taskType 敏感性：Contract-out（输出契约） 的 schema（模式） 随 taskType 变化——field_based（基于字段） 任务的 contract-out（输出契约） 包含明确的字段集（如 {category（类别）, amount（金额）, time（时间）}），topic_based（基于主题） 任务的 contract-out（输出契约） 包含主题边界内的自由文本或结构化创作输出（如 {title, lines}）。两种模式的 contract-out（输出契约） 都必须是结构化 JSON，但字段定义方式不同。

topic_based 契约的双载体定义：topic_based 的"契约"不仅包括 contract-out JSON（格式约束），还包括 topicEvolution（事件序列 + 状态快照）。field_based 的契约是字段集（contract-out JSON），topic_based 的契约是事件序列 + 状态快照（topicEvolution）。两者都是跨节点传递的标配载体，不是可选附件。

**turnType（轮次类型） 统一格式：**

```json
{
  "turnType": "ask | reply | complete | off-task | giveup | validation_failed",
  "askingField": "null | category | amount | time | quantity | character_motivation | plot_conflict | ...",
  "message": "模型对用户说的话（自然语言）",
  "validationResult": "null | { \"field\": \"amount\", \"issue\": \"amount_too_large\", \"userInput\": \"一百万\" }",
  "collectedFields": {
    "category": "猫粮",
    "amount": null,
    "time": null
  },
  "offTaskInput": "null | 用户偏离输入",
  "result": "null | { \"category\": \"猫粮\", \"amount\": 35, \"time\": \"昨天\" }",
  "changeLevel": "null | major | minor | invalid",
  "changeLevelReason": "null | 变更原因说明"
}
```

turnType="ask（追问）"：模型在追问用户，askingField（追问字段） 为当前追问的字段名（field_based（基于字段））或主题标签（topic_based（基于主题）），供 context-manager（上下文管理器） 硬门控保护使用

turnType="reply（回复）"：模型在回复用户（非追问），askingField（追问字段） 为 null（空）

turnType="complete（完成）"：所有字段齐全（field_based（基于字段））或输出满足主题边界（topic_based（基于主题）），result（结果） 包含完整结构化结果

turnType="off-task（偏离任务）"：检测到偏离，offTaskInput（偏离任务输入） 包含偏离输入

turnType="giveup（放弃）"：用户放弃

turnType="validation_failed（校验失败）"：值域校验失败（field_based（基于字段））或格式校验失败（topic_based（基于主题）），askingField（追问字段） 为校验失败的字段名或主题维度

注：`changeLevel` 和 `changeLevelReason` 描述本轮变更的信息净价值（major/minor/invalid），仅在内容发生变更时输出，null 表示无变更。与 `turnType` 独立判定——turnType=complete 时 changeLevel 可能为 major（结构性变更后完成）或 minor（微调后完成）。

**askingField（追问字段） 双模式语义：**

| taskType（任务类型） | askingField（追问字段） 取值 | 示例 | context-manager（上下文管理器） 锚点 | 执行方式 |
|---------------------|-------------------------|------|----------------------------------|---------|
| field_based（基于字段） | 预定义字段名，取值来自环节宪法 @section field-rules | "amount"、"time"、"category" | 字段级硬门控——保护与 askingField 同名的历史轮次不被淘汰 | DET 确定性字符串匹配 |
| topic_based（基于主题） | 模型动态生成的主题锚点标签，格式为 snake_case 英文标签 | "character_motivation"、"plot_conflict"、"hypothesis_A" | 主题级硬门控——保护与 askingField 语义相关的历史轮次不被淘汰 | embedding 两阶段检索 |

askingField（追问字段） 主次标注规则：强收敛任务中，如果几个字段是强关联的（如"金额+币种"），可以一轮问两个，但要在 askingField 里标清楚主次，格式为 "amount+currency"（主字段在前，次字段在后）。topic_based（基于主题） 任务中，askingField 通常为单个主题标签，但也可以标注主次（如 "character_motivation+backstory"）。

askingField（追问字段） null（空） 降级规则（两种模式通用）：当 askingField（追问字段） 为 null（空） 时（如 reply（回复） 轮次、complete（完成） 轮次），硬门控保护降级为纯语义匹配。field_based（基于字段） 模式不执行字段名匹配保护，topic_based（基于主题） 模式不执行主题标签匹配保护，均改为 context-manager（上下文管理器） 按综合匹配度评分分配形态。降级不报错不阻断——reply（回复）/complete（完成） 轮次本来就不需要硬门控保护。

### 8.2 跨节点契约继承（可选能力）

在物理隔离上下文 Γ 的同时，允许用户将旧节点的 contract-out（输出契约） 选择性传递给新节点的 contract-in（输入契约）：

默认行为：不传递（保持纯净初始态）

启用条件：用户主动声明"保留 XX 信息"，或场景需要跨环节传递上下文（如"记完账后顺便查一下余额"）

传递内容：旧节点 contract-out（输出契约） 中对应字段，非全部上下文

注入上限：不超过新节点 contract-in（输入契约） 总字段数的 30%。此为工程经验值——超过此比例时下游节点的认知负担开始显著增加、幻觉率上升。具体阈值可根据节点复杂度和模型上下文窗口调整，30% 为默认建议上限，可通过 tunable（可调参数） 槽位配置（见 §10 contract_inheritance_limit）

注入前校验：需经过根宪法校验

taskType 跨环节传递：如果旧节点是 field_based（基于字段）、新节点是 topic_based（基于主题）（或反之），传递的字段需做语义映射——field_based（基于字段） 的结构化字段注入 topic_based（基于主题） 时，作为主题空间的约束条件注入；topic_based（基于主题） 的创作输出注入 field_based（基于字段） 时，提取可结构化的字段注入。跨 taskType（任务类型） 传递需在环节宪法中声明映射规则。

### 8.3 契约演化版本管理

| 维度 | 规范 |
|------|------|
| 版本号格式（契约 schema） | semver（语义版本）（major.minor.patch）——用于契约 schema 版本 |
| 向后兼容定义 | 新增可选字段=patch（补丁版本）；新增必填字段=minor（次版本）；删除/重命名字段=major（主版本） |
| 转换时机 | 新版本部署后，存量会话在进入下一个节点时执行转换 |
| 转换失败处理 | 降级至 CLARIFYING（澄清态），通知用户 |
| EXECUTING（执行态） 节点例外 | EXECUTING 节点的契约版本转换在该节点操作完成后执行，不中断当前执行。若操作完成后转换失败，降级至 CLARIFYING |
| taskType 变更 | taskType 变更视为 major（主版本） 版本变更——因为 taskType 变更导致 askingField（追问字段） 语义、context-manager（上下文管理器） 锚点模式、DET 复验方式全部变化，不向后兼容 |


## 九、元指令与动态节点切换

### 9.1 元指令识别机制

元指令集采用"第0层全局硬匹配"定位，在 LISTENING（监听态）→ANALYZING（分析态） 之间插入 DET 预处理步骤。机制分两层：

**口令层（DET 预处理 + 精确匹配）：** 对用户原始文本执行口令精确匹配（基于架构机制 M1 的口令词表）。匹配方式：

- **精确匹配（EXACT_MATCH）：用户输入与口令词表项完全一致**（如用户输入 "@@exit" 匹配口令 "@@exit"）→ 直接触发节点切换，0 次 LLM。此为口令设计的目标——用户主动说出系统约定令牌 = 明确切换意图
- **非精确匹配 → 不切：** 任何非精确匹配的输入（含"退出""算了""不玩了"等自然语言）**一律不做口令匹配**，放行至 ANALYZING

**语义确认层（ANALYZING 顺带判断 + 二次确认）：** 自然语言中可能表达切换意图的输入（如"算了不聊柜子了，帮我写首诗""我不想继续了"），由 ANALYZING 的 LLM 在意图识别时顺带判断。若 LLM 判定用户想退出/切换：

- **不直接切**，返回 intent="meta_suspect"，message 含二次确认语（如"你是想退出当前的任务吗？退出后之前的进度不会保存。"）
- 用户明确回复"是"/"对"/"确认" → 触发节点切换
- 用户回复其他内容 → 继续当前任务

**口令精确匹配的纪律：** 口令是用户主动发出的切换信号，不需要 undo（用户说口令 = 用户主动想切，不存在误切）。自然语言二次确认路径也不需 undo（用户确认后才切）。快照机制仅适用于系统崩溃恢复，不适用于元指令路径。

### 9.2 元指令触发行为

元指令识别后（口令精确匹配 or 语义确认层用户确认切），系统不将其作为任务内容处理，直接触发节点切换。元指令集以口令词表+语义模板形式定义在架构机制（M1）中，不接受动态覆盖。

### 9.3 元指令切换时的 taskType 变化

元指令触发的节点切换可能伴随 taskType（任务类型） 变化（如从 field_based（基于字段） 的记账切换到 topic_based（基于主题） 的写诗）。切换时物理隔离当前上下文 Γ，新节点按自己的 taskType 重新初始化 askingField（追问字段） 语义和 context-manager（上下文管理器） 锚点模式。

### 9.4 房间切换与 S3 物理隔离（v5.2 新增）

态控架构的"房间"概念不仅是逻辑分组——M1 的三个核心元指令（wake/switch/exit）通过 S3 机制在物理层实现房间间的上下文隔离。这是态控架构区别于所有传统 Agent 框架的核心能力：**单一窗口内，多个房间之间通过 KV Cache 物理释放实现零泄漏切换**。

**三个元指令的 S3 行为**：

| 元指令 | KV Cache 行为 | contractStore 行为 | 新推理会话 |
|------|:---:|------|:---:|
| wake（唤醒，如"大猩猩慢慢醒"） | 从无到有，创建新会话 | 查询 contractStore → 恢复上次该房间的 sessionCheckpoint（如有）+ roomConversationLog | ✅ 创建 |
| switch（切断房间） | **释放当前房间 KV Cache**（`abort_request`）→ 创建新会话 | 归档当前房间（sessionCheckpoint + roomConversationLog）→ 从 contractStore 读取目标房间 checkpoint | ✅ 创建（新房间独立会话） |
| exit（退出，如"大猩猩，飞走吧"） | 释放当前 KV Cache | 归档当前房间 | ❌ 不创建，回 IDLE |

**与传统方案的对比**：

| 维度 | 传统 Agent 框架（LangChain/CrewAI） | 态控架构 v5.2 |
|------|------|------|
| 房间切换方式 | 用户手动新开浏览器窗口/tab | 口令触发，0 次额外操作 |
| 上下文隔离 | 浏览器进程隔离（粗糙，无法共享数据） | **S3 物理 KV Cache 释放**（精确，零泄漏） |
| 跨房间数据共享 | 无——不同窗口互相不可见 | contractStore 按 roomId 索引，任意房间可查询 |
| LLM 视角 | 每个窗口独立的"超大上下文" | 每个房间独立推理会话，调度器统一管理，**前序房间的上下文在物理上已不存在于 GPU 显存中** |
| 可靠性保证 | 依赖 prompt 层面的请求（"请忽略之前的对话"） | 架构强制——KV Cache 已清零，模型不可能看到旧上下文 |

**实现路径**：

- **自建推理引擎（vLLM/SGLang）**：调度器调用 `vLLM.abort_request(roomA_session_id)` 实现完整 S3 物理释放
- **云 API（DeepSeek/OpenAI）**：不传旧 messages[]，功能等价——LLM 物理上看不到旧房间上下文
- 开销：`abort_request` + 创建新会话 ≈ 几十毫秒，远小于一次 LLM 调用的延迟


## 十、受控调参系统（tunable 槽位）

宪法中可声明运行时允许调整的参数，含 type（类型）/range（范围）/enum（枚举）/default（默认值）/conflict_check（冲突检测）。由系统设置界面自动生成配置表单，冲突检测引擎在保存前校验。参数调整后下一轮新会话生效，变更日志不可篡改。tunable（可调参数） 的灵活性是"受控的灵活性"——适应性受限，但可控、可审计、可测试。

与纯 LLM 运行时动态调整的区别：

| 维度 | 纯 LLM 动态调整 | 态控 tunable 槽位 |
|------|--------------|-----------------|
| 调整方式 | 在 prompt 里写条件 | 在系统设置里改参数 |
| 调整范围 | 无限制 | range/enum 硬限制 |
| 冲突检测 | 无 | 确定性代码检测 |
| 审计 | 难 | 变更日志，每次调整可追溯 |
| 生效时机 | 下一轮对话立即生效 | 保存后下一轮新会话生效 |

**关键 tunable（可调参数） 参数清单：**

| 参数名 | 默认值 | 取值范围 | 用途 | 消费方 | 适用 taskType |
|--------|--------|---------|------|--------|-------------|
| amount_limit_max（最大金额限制） | 999999 | [0, 999999999] | 金额上限（值域校验） | 模型第一道 + 调度器DET 第二道 | field_based（基于字段） |
| turnHistory_limit（轮次历史限制） | 20 | [5, 100] | roomConversationLog 保留轮数（normal 环节基数；critical 环节 = 基数 × critical_room_history_boost；high 环节 = 基数 × 0.6；low 环节 = 基数 × 0.2） | session-store（会话存储） | 通用 |
| critical_room_history_boost（关键房间历史增强系数） | 2.0 | [1.0, 5.0] | critical 环节的 roomConversationLog 保留轮次 = turnHistory_limit × 此系数。正常 20 轮，critical 默认 40 轮 | session-store（会话存储） | 通用 |
| pending_fields_ttl（待定字段存活时间） | 24h | [1h, 168h] | 半成品记录暂存时长（对应 §6.2 _pendingCollectedFields 的存活时间） | session-store（会话存储） | 通用 |
| boundary_coverage_threshold（边界覆盖阈值） | 0.85 | [0.50, 1.00] | 边界覆盖完整性检查的通过阈值 | 设计阶段验证（N2 覆盖率校验） | 通用 |
| logprobs_threshold（logprobs阈值/v5.1修正） | 0.4 | [0.0, 1.0] | logprobs 检查阈值——ANALYZING 从 API logprobs 获取真实概率分布，DET 将 probability 与此阈值比较，低于此值且 message 含"硬撑式确认"词表→拦截。原 confidence 因 LLM 自由生成不可靠已废弃 | DET 复验 | 通用 |
| contract_inheritance_limit（契约继承限制） | 0.3 | [0.1, 0.5] | 跨节点契约继承注入上限（占新节点 contract-in 总字段数的比例，对应 §8.2 的 30% 上限） | 契约继承模块 | 通用 |
| topic_anchor_similarity_threshold（主题锚点相似度阈值） | 0.6 | [0.0, 1.0] | topic_based（基于主题） 模式主题级硬门控 embedding 相似度阈值 | context-manager（topic_based 模式阶段2） | topic_based（基于主题） |
| topic_keyword_overlap_threshold（主题关键词重叠阈值） | 0.2 | [0.0, 1.0] | topic_based 模式 DET 关键词粗筛重叠度阈值 | context-manager（topic_based 模式阶段1） | topic_based（基于主题） |
| topic_evolution_match_threshold（主题演化匹配阈值） | 0.65 | [0.0, 1.0] | topicEvolution 跨会话匹配 embedding 相似度阈值。用户说"继续上次那个故事"时，ANALYZING 用此阈值匹配已有的 topicEvolution 记录。略高于 topic_anchor_similarity_threshold（0.6），因为跨会话匹配需要更高精度 | context-manager（topic_based 模式初始化阶段） | topic_based（基于主题） |
| topic_evolution_minor_compaction_threshold（主题演化 minor 压缩阈值） | 5 | [3, 20] | minor 级 event 累计到 N 条时触发汇总压缩 | 调度器 | topic_based（基于主题） |
| **domain_rule_validation_threshold**（v4.2 新增） | **0.7** | **[0.5, 0.9]** | **候选规则通过交叉验证的最低验证通过率** | **异步提炼引擎** | **通用（topic_based 按 topicPath，field_based 按 stepName）** |
| **rule_mining_schedule**（v4.2 新增） | **"0 2 * * *"** | **cron 表达式** | **规则提炼时间窗口触发调度（cron 表达式，默认每日凌晨2点）** | **异步提炼引擎** | **通用** |
| **rule_mining_batch_threshold**（v4.2 新增） | **5** | **[3, 20]** | **规则提炼数量阈值触发（积累 N 个同类 changeLog 时触发）** | **异步提炼引擎** | **通用** |
| **rule_mining_batch_size**（v4.2 新增） | **10** | **[5, 50]** | **每次批量提炼的最大规则数** | **异步提炼引擎** | **通用** |
| **session_checkpoint_ttl**（v4.3 新增） | **604800** | **[86400, 2592000]** | **field_based 进度检查点过期时间（秒，默认 7 天）。过期时联动清理 roomConversationLog** | **态控调度器恢复检查** | **field_based** |
| cold_start_observation_window（冷启动观测窗口） | 50 | [20, 200] | 冷启动观测窗口会话数（对应 §6.1.5 的 cold_start_observation_window——系统/环节启动后的前 N 个会话为冷启动期） | 冷启动监测器 | 通用 |
| cold_start_emergency_threshold（冷启动应急解冻阈值） | 0.5 | [0.3, 0.8] | 冷启动应急解冻触发阈值（如 logprobs 拦截率>50% 触发应急解冻） | 冷启动监测器 | 通用 |
| pid_kp（P 控制比例系数） | 0.1 | [0.01, 1.0] | P 反馈控制比例系数（参数调整 = Kp × (目标值 - 实际值)） | 自动调参引擎 | 通用 |
| **strengthens_weight_cap**（v4.6 新增） | **3** | **[1, 10]** | **同一目标规则被多条 strengthens 边指向时的权重累加上限。超过 cap 的部分不再计入排序权重——避免一条规则被 N 条 strengthens 加权后垄断排序首位。详见 §3.7.6** | **上下文管理器（规则注入排序）** | **通用** |
| **changelevel_major_sample_rate**（v4.6 新增） | **0.05** | **[0.01, 0.20]** | **changeLevel major 标记的抽样人工复核比例。冷启动期间（cold_start_observation_window 内）临时上调至 0.10。详见文末风险条目** | **抽样人工复核引擎** | **通用** |
| **max_critical_rooms**（v4.7 新增） | **3** | **[1, 10]** | **预计同时活跃的 critical 房间数上限，用于 §10.1 上下文预算约束计算。非硬限制——超出时仅触发 cross_param warning，不阻止运行** | **上下文管理器（§10.1 约束校验）** | **通用** |

冷启动冻结机制：
冷启动期间参数冻结，但实时观测指标：监测 logprobs 拦截率、topicEvolution event 写入频率、minor 压缩频率、规则提炼触发频率等

应急解冻机制：若指标异常（如 logprobs 拦截率 > cold_start_emergency_threshold），触发"应急解冻"——人工确认后调整参数

解冻后 P 反馈控制自动调节：参数调整 = pid_kp × (目标拦截率 - 实际拦截率)。例如 logprobs 拦截率目标 5%，实际 15%，调整 = 0.1 × (0.05 - 0.15) = -0.01，logprobs_threshold 下调 0.01

> 注：当前版本仅启用比例项（Kp），积分项（Ki）和微分项（Kd）预留未启用。如需消除稳态误差可引入 Ki，如需抑制超调可引入 Kd，但当前 P 控制已满足参数微调需求。

所有调整记录变更日志：时间戳、操作者、参数名、旧值、新值、调整原因、触发指标

默认值说明要点：
v5.1 logprobs_threshold（logprobs阈值）默认 0.4——ANALYZING 从 API logprobs 获取真实概率分布，0.4 可有效拦截低概率的"硬撑"。冷启动观测窗口内冻结，应急解冻可应对明显错误。（v5.1修正：原 confidence_threshold 因 LLM 自由生成 confidence 不可靠已废弃）
v4.0.2 topic_anchor_similarity_threshold（主题锚点相似度阈值） 默认 0.6——embedding 相似度比 LLM 语义判断更稳定，0.6 是平衡保护力度和上下文窗口占用的经验值。过低（如 0.3）会导致过多历史轮次被保护，上下文窗口膨胀；过高（如 0.8）会导致保护力度不足，模型重复追问已讨论过的主题。
v4.0.3 topic_keyword_overlap_threshold 默认 0.2——DET 粗筛阈值，过低（如 0.1）粗筛通过太多失去意义，过高（如 0.5）粗筛漏报多。
v4.0.4 topic_evolution_match_threshold 默认 0.65——略高于 topic_anchor_similarity_threshold（0.6），因为跨会话匹配需要更高精度：误匹配的代价是"把用户的新故事错误地续写到旧故事上"，比环节内的锚点漏判（重复追问）更严重。过低（如 0.5）会导致误匹配率高，过高（如 0.8）会导致"继续上次那个故事"无法匹配已有 topicEvolution。

### 10.1 跨参数联动约束规则（v4.7 新增）

定位：扩展 §C.2 冲突检测引擎，处理 tunable 参数之间的隐式语义依赖。与 single_param range/enum 校验不同——cross_param 不替用户决策，只做语义校验 + 建议 + 警告。

**规则格式规范：**

| 字段 | 必填 | 说明 |
|------|------|------|
| `type` | 是 | 固定值 `"cross_param"` |
| `name` | 是 | 规则标识符，snake_case |
| `violation_action` | 否 | 默认 `"warning"`——cross_param 违规不阻止保存。可选 `"hard_reject"`（用于确定性越界） |
| `params` | 是 | 被此规则引用的 tunable 参数名数组——仅这些参数变更时重新执行此规则 |
| `constants` | 否 | 计算常量（如模型窗口大小、每轮平均 token 数），声明在此以供引用 |
| `compute` | 是 | 沙箱表达式，仅允许 `+ - * / floor ceil min max` 运算符 + params 变量 + constants 常量。求值结果 true=违规触发 |
| `event` | 是 | 违规事件结构：`{type, severity, message, suggested_adjustment}`。`suggested_adjustment` 可含带 formula 的建议值计算提示 |

**初始规则清单（5 条）：**

```json
{
  "conflict_rules": [
    {
      "type": "cross_param",
      "name": "critical_room_context_budget_check",
      "violation_action": "warning",
      "params": ["turnHistory_limit", "critical_room_history_boost", "max_critical_rooms"],
      "constants": {
        "avg_tokens_per_turn": 200,
        "model_token_budget": 128000
      },
      "compute": "max_critical_rooms * turnHistory_limit * critical_room_history_boost * avg_tokens_per_turn > model_token_budget * 0.6",
      "event": {
        "type": "cross_param_warning",
        "severity": "warning",
        "message": "当前参数组合下，max_critical_rooms 个 critical 房间的历史记录可能占用过多 token 预算。",
        "suggested_adjustment": "建议将 turnHistory_limit 降至 floor(model_token_budget * 0.6 / max_critical_rooms / critical_room_history_boost / avg_tokens_per_turn) 或降低 critical_room_history_boost"
      }
    },
    {
      "type": "cross_param",
      "name": "topic_threshold_ordering_check",
      "violation_action": "hard_reject",
      "params": ["topic_anchor_similarity_threshold", "topic_evolution_match_threshold"],
      "compute": "topic_anchor_similarity_threshold >= topic_evolution_match_threshold",
      "event": {
        "type": "cross_param_conflict",
        "severity": "error",
        "message": "topic_anchor_similarity_threshold 必须严格小于 topic_evolution_match_threshold——跨会话匹配需要比环节内锚点更高的精度。参见 v4.0.4 说明。"
      }
    },
    {
      "type": "cross_param",
      "name": "cold_start_window_vs_emergency_threshold_check",
      "violation_action": "warning",
      "params": ["cold_start_observation_window", "cold_start_emergency_threshold"],
      "compute": "cold_start_observation_window < 30",
      "event": {
        "type": "cross_param_warning",
        "severity": "warning",
        "message": "冷启动观测窗口过小（< 30），统计量不足以支撑 emergency_threshold 的准确判断。建议将 cold_start_observation_window 至少设为 30。"
      }
    },
    {
      "type": "cross_param",
      "name": "rule_mining_dual_trigger_too_conservative_check",
      "violation_action": "warning",
      "params": ["rule_mining_schedule", "rule_mining_batch_threshold"],
      "constants": { "one_day_seconds": 86400 },
      "compute": "rule_mining_batch_threshold > 15",
      "event": {
        "type": "cross_param_warning",
        "severity": "warning",
        "message": "rule_mining_batch_threshold 设置过高（> 15），配合时间窗口触发可能导致规则提炼长期不触发。建议降至 10 或以下。"
      }
    },
    {
      "type": "cross_param",
      "name": "logprobs_cold_start_freeze_consistency_check",
      "violation_action": "warning",
      "params": ["cold_start_observation_window", "logprobs_threshold"],
      "compute": "cold_start_observation_window > 150",
      "event": {
        "type": "cross_param_warning",
        "severity": "warning",
        "message": "冷启动观测窗口设得很大（> 150），logprobs_threshold 将长期冻结在初始值。如果初始值不合适，系统会在很长一段时间内用错误的阈值运行。建议将 window 控制在 50-100 之间。"
      }
    }
  ]
}
```

**维护纪律：**

- 新增 tunable 参数时同步审查是否需要新增跨参数约束——任何与其他参数存在语义依赖的参数都应至少有一条 cross_param 规则
- 约束不应过度拟合当前模型特性作为常数（如 `model_token_budget=128000`）——此类常数应在 tunable 参数表中暴露为可配置项或至少以 constants 形式集中声明，便于模型升级时统一修改
- 两条参数之间如果存在确定性大小关系（如 topic_anchor_similarity_threshold < topic_evolution_match_threshold），应使用 `hard_reject` 而非 `warning`——这是硬约束，不是警告
- 跨参数约束只覆盖"参数组合可能导致系统行为异常"的情况，不覆盖"这个参数值可能不适合你的业务场景"——后者属于场景设计阶段的人为判断，不是冲突检测引擎的职责

## 附录

### 附录 A：高风险场景降级链参考

对于医疗预问诊、复杂客服、金融风控等高危场景，默认的四项降级链（L1 结构校验 + DET 值域复验 + logprobs 检查 + 硬编码兜底）是通用底线，不可缩减。在此基础上，可追加以下增强方案：

```
simple: [L1_retry, DET_value_recheck, logprobs_check, cross_task_extension_check, hardcoded]
escalated: [L1_retry, DET_value_recheck, logprobs_check, cross_task_extension_check, hardcoded, human_takeover]
human_takeover: { target: "人工队列", sla: "5分钟" }
```

启用条件：仅当场景的"错误代价"判据为"代价大且无客观答案"或"代价大但有客观校验"时，考虑启用 escalated 模式。默认场景仍用 simple（四项降级链 + 硬编码兜底）。

注意：此方案为场景实例化时的参考，不进宪法模板。启用 escalated（升级） 模式时，需在场景的边界清单中显式声明人工接管的目标队列和 SLA（服务等级协议）；并联模式下，human_takeover（人工接管） 事件由 Case Supervisor（案例监督器） 路由到人工审批队列，而非当前会话的 UI（用户界面）。四项降级链不可被 human_takeover（人工接管） 替换——human_takeover（人工接管） 是五项之后的扩展层，不是替代。

### 附录 B：多模型 A/B 测试路由配置示例

态控架构原生支持多模型 A/B 测试，无需部署两套系统。以下为路由表（路由表）配置示例：

> 注：下表中的 VALIDATING_A / VALIDATING_B / COMPARING 为 A/B 测试专用扩展状态，不属于 §4.2.1 标准状态集。仅在启用 A/B 测试扩展时生效。

| 当前状态 | contract-out（输出契约） 关键字段 | 下一状态 | 路由规则 | 模型选择 |
|---------|--------------------------------|---------|---------|---------|
| IN_SESSION | turnType（轮次类型）="complete" + intent="generate_copy（生成文案）" | VALIDATING_A（校验A） | 硬匹配 | 模型A（对照组） |
| IN_SESSION（会话中） | turnType（轮次类型）="complete" + intent="generate_copy（生成文案）" | VALIDATING_B（校验B） | 硬匹配 | 模型B（实验组） |
| VALIDATING_A（校验A） | valid（有效）=true | COMPARING（对比） | 硬匹配 | — |
| VALIDATING_B（校验B） | valid（有效）=true | COMPARING（对比） | 硬匹配 | — |
| COMPARING（对比） | — | PREVIEWING（预览态）（IN_SESSION 子环节） | 总是 | DET 节点对比指标 |

对比节点（COMPARING（对比））为确定性程序，不调 LLM。对比指标可包括：
输出字段完整度（必填字段缺失率）——field_based（基于字段） 适用
格式合规率（Schema（模式） 校验通过率）——两种模式均适用
主题相关性评分（topic_based（基于主题） 适用，需 LLM 辅助评分）
历史用户确认率（同一意图下用户直接确认 vs 驳回的比例）——两种模式均适用

若模型 B 在所有指标上优于模型 A 且达到预设阈值，系统可自动将路由表中的模型选择切换为模型 B，或触发人工确认后切换。两个模型收到完全相同的 contract-in（输入契约），受完全相同的局部宪法约束，输出完全相同的 schema（模式）——对比不是靠人工评估，而是靠 DET 节点对两个结构化输出的质量指标做确定性比较（topic_based（基于主题） 的主题相关性评分除外，需 LLM辅助）。

### 附录 C：工程实施规范

#### C.1 执行体调用链追踪（OpenTelemetry 集成）

问题背景：三类执行体（DET/LLM/HUMAN）的调用缺少全链路追踪，运维监控和故障定位困难。

实施规范：
每次 DET/LLM/HUMAN 调用生成 OpenTelemetry span
span 含字段：trace_id / span_id / parent_span_id / executor_type（DET/LLM/HUMAN）/ executor_name / start_time / end_time / status（success/fail/timeout）/ input_summary / output_summary / error_detail
用 OpenTelemetry 标准格式上报（Jaeger/Zipkin 兼容）
运维侧用 Jaeger/Zipkin 可视化调用链
监控指标（校验失败率/偏离率/logprobs 拦截率/embedding 调用次数/topicEvolution event 写入频率/冷启动应急解冻触发次数）直接从 trace 提取
每个会话一个 trace，每个节点一个 span，每个执行体调用一个子 span

集成位置：
第0层全局硬匹配：DET span
第0.5层关键词扫描：DET span
第1层意图识别：LLM span
第2层环节内自治：LLM span（环节内多轮对话一个 span，含 turn_count 子字段）
第3层 DET 复验：DET span（含四项检查各自的子 span）
EXECUTING：DET span
embedding 调用（topic_based 模式阶段2）：embedding span（含 askingField / similarity_score / protected_count 子字段）
topicEvolution event 写入：DET span（含 topicId / eventId / nodeId / stateChange 子字段）

收益：运维侧可实时观测执行体调用链，快速定位故障节点；监控指标从 trace 自动提取，无需额外埋点。

#### C.2 宪法冲突检测引擎

实施规范：
- `single_param` 规则用 json-rules-engine（Node.js）或 Drools（Java）实现确定性 range/enum 校验
- `cross_param` 规则（v4.7 新增，详见 §10.1）由冲突检测引擎在遍历 single_param 完成后执行 compute 表达式求值——用沙箱 `eval()`（仅允许 `+ - * / floor ceil min max` 运算符 + params 变量 + constants 常量）
- 参数变更时通过 `params` 字段做依赖追踪——仅被引用参数的变更触发对应 `cross_param` 规则重新执行
- `violation_action` 默认值：`single_param` 规则默认为 `hard_reject`；`cross_param` 规则默认为 `warning`
- `violation_action=hard_reject` 行为：拒绝保存 + 返回错误详情
- `violation_action=warning` 行为：弹窗展示 warning message + suggested_adjustment，用户可选择"仍然保存"或"调整后重试"。warning 不阻止保存
- compute 表达式求值失败（语法错误、引用不存在参数）→ 视为 `hard_reject`，阻止保存并返回明确的表达式错误位置
- conflict_check 规则用 JSON 格式声明，与 tunable 参数一同存在 tunables.json
- 保存前执行全部规则：`single_param` 任一 hard_reject → 拒绝保存；`cross_param` warning → 弹窗但允许继续
- 冲突检测引擎是确定性代码，不调 LLM

完整 cross_param 规则清单见 §10.1。

规则示例：

```json
{
  "conflict_rules": [
    {
      "type": "single_param",
      "name": "logprobs_threshold_range_check",
      "condition": {
        "all": [
          { "fact": "parameter", "path": "$.logprobs_threshold", "operator": "lessThan", "value": 0 },
          { "fact": "parameter", "path": "$.logprobs_threshold", "operator": "greaterThan", "value": 1 }
        ]
      },
      "event": { "type": "conflict", "message": "logprobs_threshold 必须在 [0, 1] 范围内" }
    },
    {
      "type": "cross_param",
      "name": "critical_room_context_budget_check",
      "violation_action": "warning",
      "params": ["turnHistory_limit", "critical_room_history_boost", "max_critical_rooms"],
      "constants": {
        "avg_tokens_per_turn": 200,
        "model_token_budget": 128000
      },
      "compute": "max_critical_rooms * turnHistory_limit * critical_room_history_boost * avg_tokens_per_turn > model_token_budget * 0.6",
      "event": {
        "type": "cross_param_warning",
        "severity": "warning",
        "message": "当前参数组合下，3 个 critical 房间的历史记录可能占用过多 token 预算",
        "suggested_adjustment": "建议将 turnHistory_limit 降至 floor(model_token_budget * 0.6 / max_critical_rooms / critical_room_history_boost / avg_tokens_per_turn) 或将 critical_room_history_boost 降低"
      }
    }
  ]
}
```

#### C.3 contractStore 物理存储（SQLite + WAL 模式）

问题背景：contractStore 跨会话持久化，需要支持并发和崩溃恢复。

实施规范：
SQLite 单文件数据库，每个用户/项目一个 .db 文件
WAL（Write-Ahead Logging）模式支持并发读 + 单写
数据库文件路径：<project_root>/.contractstore/<user_id>.db

contractStore 表结构：

| 表名 | 用途 | v4.2 变更 |
|------|------|----------|
| analyzing_contract_in | ANALYZING 节点的原始 contract-in 副本（支持意图级纠错逃生舱） | 不变 |
| analyzing_contract_out | ANALYZING 节点的 contract-out 副本 | 不变 |
| topicEvolution | topic_based 任务的主题演化表，记录 topicId、stateSnapshot（active/checkpoint/abandoned）、创建时间、更新时间 | 不变 |
| topicEvolutionEvent | topicEvolution 的事件子表，记录 eventId、topicId（外键）、nodeId、turnType、timestamp、stateChange（含 before/after/delta）、askingField、modelOutputSummary | 不变 |
| **domainRules** | **领域规则表** | **v4.2：新增 edges 字段（JSON 数组），存储 depends_on/conflicts_with/strengthens 三种关联边（命名与 §3.7.4 一致，snake_case + 主动语态）；新增 topicPath 字段（JSON 数组）、conditions 字段（JSON 对象）、immutableLevel 字段（L0-L3）、source 字段、version 字段** |
| **ruleCandidates** | **候选规则池** | **v4.2 新增：待交叉验证的规则，含 candidateId、rule、evidenceCount、passCount、validationRate、status（pending/confirmed/rejected）** |
| **ruleMiningQueue** | **规则提炼待处理队列** | **v4.2 新增：checkpoint/abandoned 时推入，含 queueId、topicId、intent、checkpointAt、status（pending/processing/done）** |
| **sessionCheckpoints** | **field_based 进度检查点** | **v4.3 新增：含 userId、lastCompletedStep、completedSteps、stepSnapshots，ttl 默认 7 天。用户恢复时注入 contractOut 供 N4 路由** |
| **roomConversationLog** | **房间对话日志** | **v4.3 新增：按 userId+roomId 实时记录每轮对话，支持全文搜索。返回房间时注入最近 N 轮。切换房间不丢弃内容。保留期与 sessionCheckpoint.ttl 联动——checkpoint 过期时，对应 userId 所有 roomConversationLog 也清理** |
| topicEvolutionArchive | 归档表（minor 级原始 event + invalid 级 event） | 不变 |
| **outputRegistry** | **产出物总索引** | **v5.2 新增：全系统产出物统一登记簿。含 outputId、roomId、outputType、outputPath、outputName、outputSummary、keywords 等字段。FTS 全文检索索引。详见 §3.1.3** |

并发控制：
WAL 模式支持多读单写
写操作用事务包裹，崩溃时自动回滚
多用户并发时，每个用户独立 .db 文件，无跨用户锁竞争

### 附录 D：align-session 可选扩展规范

align-session 是可选扩展，非态控架构标准能力。若场景含意图校准需求，可启用 align-session 扩展。启用时需：
额外编写 align-session.json 宪法
在路由表中增加 align 相关条目
在 tunable 参数中增加 divergence_threshold
同步更新场景的边界清单
在 N5 第0层之后插入第0.1层（align 静默模式触发与处理）

align-session 核心机制（仅在启用时生效）：
静默输入：/align 元指令触发后，挂起语义管道，所有输入走静默缓冲区，直到完成指令或退出词
多模型并行仲裁：主次模型并行 + 规则模型串行
锚点确认：主/次模型标注 core_anchor，用户确认后沉淀 user_confirmed_anchor
迭代裁决：divergence_threshold 判定分歧度
对齐数据包沉淀：rounds[] / divergence_points / user_arbitration

align 相关 tunable 参数（仅在启用时声明）：
divergence_threshold：默认 0.7，校准模式分歧度阈值

align-session 宪法 complete 条件：用户最终确认。

align 相关路由条目（仅在启用时配置）：
ANALYZING（intent=align）→ IN_SESSION(align)
IN_SESSION(align) complete → EXECUTING（DET）
IN_SESSION(align) giveup → LISTENING
IN_SESSION(align) off-task → ANALYZING（轻量）

与核心架构的边界：align-session 启用时不修改根宪法第 1-4 条及架构机制 M1-M4，仅在路由表和 tunable 参数中扩展，并在 N5 插入第0.1层。align-session 不启用时，核心架构完全不受影响。

---

### 风险条目：changeLevel 防偷懒

**风险描述**：模型可能偷懒，将所有变更标记为 major（保证不被压缩）或跳过 changeLevel 判定。更隐蔽的偷懒形态是"认真编一个 changeLevelReason 理由但仍然偷懒"——DET 格式校验能拦空值/超长，但挡不住"看起来合理实际仍是偷懒"的理由。

**缓解措施**：
- 根宪法第4条已要求 changeLevelReason 理由字段（见 §7.2）
- DET 格式校验拦截 changeLevelReason 空值/超长
- 监控指标：changeLevel major 占比阈值从 <1% 调整为 <3%，新增 changeLevelReason 空值率检测
- 若 major 占比超阈值或空值率异常，触发冷启动应急解冻机制（见 §10）审查参数
- **抽样人工复核机制（v4.6 新增）**：按比例抽检 major 标记的合理性，作为 DET 格式校验之上的第二道防线
  - 默认抽检比例：5%（通过 tunable 参数 `changelevel_major_sample_rate` 配置，取值范围 [0.01, 0.20]，默认 0.05）
  - 抽检对象：所有被模型标记为 major 的 changeLevel 事件
  - 复核流程：抽样事件推入人工复核队列 → 复核员判断"该变更是否真的属于结构性变更（新增/删除必采字段、修改字段值域、改变 Schema 结构）" → 标记为 confirmed_major / false_positive
  - 反馈闭环：false_positive 比例 > 30% 时，触发冷启动应急解冻机制（见 §10）审查 logprobs_threshold 与 changeLevel 判定 prompt
  - 与冷启动的关系：冷启动期间（cold_start_observation_window 内）抽检比例临时上调至 10%，冷启动结束后恢复默认 5%——冷启动期模型行为未稳定，需要更高密度的反馈信号
  - 复核结果记入变更日志：时间戳、抽检事件 ID、复核员、结论、复核理由


### 附录 E：编程规则宪法（v5.0 新增）

**定位**：约束代码模型的元规则，全局生效于整个 N1→N15 管线。任何涉及代码生成的环节（N13）必须在生成代码前校验此宪法，违反任一条款拒绝生成。

**约束对象**：代码模型（生成骨架代码的 AI），非运行时模型。

**生效范围**：母本的一部分，设计者在 P0 认知加载时即需理解，N13 代码模型强制输入。

| 编号 | 条款 | 内容 |
|:--:|------|------|
| 1 | **执行体分派铁律** | 每个状态的执行体由 v5.0/N3 决定，代码模型不得自行更改。ANALYZING=LLM，IN_SESSION=LLM，DET 复验=DET，EXECUTING=DET。不得将 ANALYZING/IN_SESSION 降级为正则或 parser |
| 2 | **DET 职责边界** | DET 只做：元指令 EXACT_MATCH（精确匹配）+ 值域复验 + 关键词扫描标记。不扩展到意图识别、字段采集、查询理解 |
| 3 | **LLM 职责边界** | LLM 做：意图识别 + 字段采集/追问/偏离判断/查询理解。不得用正则/parser 替代 |
| 4 | **降级规则** | 无 apiKey 时 ANALYZING/IN_SESSION 必须**拒绝服务并明确告知原因**，不得以任何形式降级为正则/DET 替代。降级模式如存在必须标注"降级非正道" |
| 5 | **契约对齐** | 代码必须严格对齐 N11 契约（contract-in/out schema），不自创接口 |
| 6 | **tunable（可调参数）/接口隔离** | 阈值从 getTunable() 读，调度器只调 context-manager（上下文管理器）三接口，不内联拼接逻辑 |

---

> 文档版本：v5.2
> 日期：2026-07-17
> 基于：态控架构改进方案 v1.3（改进5+改进6）
> 变更摘要：v5.1→v5.2：新增产出物总索引（Output Registry），全系统产出物统一登记簿，跨房间可发现、可检索、可自动复制，DET 确定性查询（0 LLM）。详见 §3.1.3、§2.6 自由切换第四支柱。
>
> **v5.5 全局修正声明（2026-07-17）**：① off-task保障2→ANALYZING重新识别验证；② M1新增switch；③ ANALYZING→强制选择+logprobs；④ M1触发S3物理KV Cache释放；⑤ M1注入roomStateIndex；⑥ **M2移除跨任务延伸检测——降级链从五项精简为四项。跨任务是用户显式确认行为，不需要降级链层自动化检测**。详见 §3.1.1、§6.1.3降级链、§7.3 M2定义、§9.4。
