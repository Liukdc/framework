# 节点说明 vs 总纲 + 流程指南 · 冲突审计报告

**审计日期**：2026-07-18
**审计对象**：`节点说明/` 目录（P0/N1-N15 节点文档）
**对照基准**：总纲 v5.6 + 流程指南 v8.2

---

## 一、总体评估

节点说明全部锚定在 **态控架构 v5.6 + 流程指南 v8.2**，而当前母本已演进到 v5.6 / v8.2。存在三批差距：v5.3 修订（25条）、v5.4-v5.5（tool calling + 工具分层）、v5.6（项目隔离 + 文件管理）。

**影响最严重的节点**：P0（认知加载不全）、N5（调度器缺工具调用路由 + 项目隔离）、N13（骨架代码宪法缺第7条）、N14（审骨架缺 tool calling/项目验证项）。

---

## 二、🔴 必须修复（4 条）

### N1. P0 认知加载的"十件套"缺 v5.3+ 关键认知

**位置**：`P0_节点说明_认知加载_v5.0.md` 第 15-21 行、`v6.0` 全量版第 18-22 行

**现状**：P0 要求设计者掌握的认知清单包含"编程规则宪法六条"、引用"态控架构 v5.6"。

**缺失项**：
- 编程规则宪法应为 **七条**（缺 v5.5 工具分层发现）
- 态控架构版本应为 v5.6
- 缺 IN_SESSION tool calling (v5.4)
- 缺 工具分层发现三体系 (v5.5)
- 缺 项目隔离机制 (v5.6)
- 缺 文件管理规范 (v5.6)

### N2. N5 调度器设计缺 tool calling 路由 + 项目隔离

**位置**：`N5_节点说明_v5.9.md`（835行，关键节点）

**现状**：引用"态控架构 v5.6 + 流程指南 v8.2"，无 tool_calling、项目隔离相关内容。

**缺失项**：
- 调度器 state 数据未包含 `currentProjectId`、`toolRegistry`
- 路由表缺少工具调用路由条目（IN_SESSION 触发 tool→DET 函数映射）
- 调度器未实现项目切换的四步 S3 流程

### N3. N13 骨架代码宪法缺第 7 条

**位置**：`N13_节点说明_v1.2.md`（316行）、`N13_环节宪法_v1.2.md`

**现状**：N13 是代码生成节点，必须在生成前校验编程规则宪法。但当前版本引用的是旧宪法。

**缺失项**：
- 宪法校验清单只有 6 条，缺第 7 条（工具分层发现）
- 代码模型不会收到"LLM 不直接写 SQL"的约束

### N4. N14 审骨架缺 v5.4+ 验证项

**位置**：`N14_节点说明_v1.2.md`（303行）

**现状**：有执行体分派静态分析 + 运行时验证（v5.0 层面），但缺：
- IN_SESSION 是否可调用 DET 工具的验证
- 工具分层发现机制是否实现的验证
- 项目隔离是否实现的验证

---

## 三、🟡 建议修复（4 条）

### N5. 全量版 v6.0 混淆了母本版本

`态控闭环系统人机设计流程_节点说明_v6.0.md` 开篇说"对齐态控架构 v5.6"，但 P0 节又引用"态控架构 v5.6"——两个版本号矛盾。

### N6. P0 v4.1 和 v5.0 两个文件并存

`P0_节点说明_认知加载_v4.1.md` 和 `P0_节点说明_认知加载_v5.0.md` 都在节点说明目录中。v4.1 的文件名暗示未更新，但内容标注"文档版本：v5.0"——文件名和内容不一致。

### N7. 节点宪法文件未更新

所有 `N*_环节宪法_v*.md` 文件没有反映 v5.4-v5.6 的新特性：
- 环节宪法模板缺 `@section tools` 和 `@section tool_catalog` 标签
- 缺项目级别的宪法隔离说明（环节宪法不分项目，但产出物分项目）

### N8. N2 的编程规则宪法参考

`N2_节点说明_v6.0.md` 负责边界划定和 taskType 初判，应包含项目隔离需求的判定——当前缺失。

---

## 四、🟢 低优先级（2 条）

### N9. L3包对照清单锚定在 v4.7

`L3包对照v4.7问题清单.md` 文件名写 v4.7，内容可能已过时。

### N10. v6.0 全量版引用的流程指南版本

v6.0 全量版中 P0 引用的"流程指南 v8.2"应更新为 v8.2。

---

## 五、各节点 v5.4+ 特性覆盖一览

| 节点 | tool_calling | 工具分层 | 项目隔离 | 文件管理 | 当前引用版本 |
|------|:--:|:--:|:--:|:--:|------|
| P0 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N1 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N2 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N3 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N4 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| **N5** | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N6 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N7 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N8 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N9 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N10 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N11 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N12 | ❌ | ❌ | ❌ | ❌ | v5.0 |
| **N13** | ❌ | ❌ | ❌ | ❌ | v5.0 |
| **N14** | ❌ | ❌ | ❌ | ❌ | v5.0 |
| N15 | ❌ | ❌ | ❌ | ❌ | v5.0 |

---

## 六、修复优先级

| 优先级 | 编号 | 问题 | 影响 |
|--------|------|------|------|
| **P0** | N1 | P0 认知加载缺 v5.3+ 认知 | 设计者从起点就学不到最新机制 |
| **P0** | N2 | N5 调度器缺 tool calling + 项目隔离 | 生成的骨架代码不可运行 |
| **P0** | N3 | N13 宪法缺第 7 条 | 代码模型不受工具分层约束 |
| **P1** | N4 | N14 审骨架缺 v5.4+ 验证 | 审骨架流程有盲区 |
| **P1** | N5 | v6.0 全量版版本号矛盾 | 混淆 |
| **P2** | N6-N10 | 其余 | 打磨 |

---

> 审计版本：v1.0
