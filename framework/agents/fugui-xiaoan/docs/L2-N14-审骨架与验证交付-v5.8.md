# L2-N14 审骨架 + 验证交付（v5.8）

**项目**：富贵小安
**基线**：态控架构 v5.8
**日期**：2026-07-20

---

## 一、审骨架 8 项

| # | 审查项 | 通过标准 | 验证方法 |
|---|--------|---------|---------|
| 1 | 状态转移图与 L2 一致 | N3 9个状态 + N4 32条转移边全部实现 | 对照 N4 transitions.json 逐条核对 scheduler.js 路由矩阵 |
| 2 | DET 双层位置正确 | 第0.5层关键词扫描在用户输入后→ANALYZING前；第3层四项检查在 complete 时 | 读 scheduler.js 代码确认调用位置 |
| 3 | askingField 在 turnType schema + buildPromptContext 参数 | 字段级硬门控消费 askingField | 读接口调用链 |
| 4 | tunable 从 getTunable() 读 | 全文搜索无硬编码阈值（0.4/20/999999 等） | grep 扫描 scheduler.js + context-manager.js |
| 5 | context-manager 三接口调用占位 | 无内联拼接逻辑（调度器中不出现 turnHistory 直接拼接） | 读 scheduler.js 代码 |
| 6 | taskType 双模式锚点实现 | field_based 执行字段级硬门控；富贵小安全 field_based | 读 buildPromptContext() |
| 7 | v5.7/v5.8 对话分段保留 | roomConversationLog 含 segmentType/relatedOutputId/summaryImportance/originalTurnRange；conversationArchive FTS5 不参与拼接 | 读 contract-store.js DDL + buildPromptContext() |
| 8 | **执行体分派验证** | 层1静态：identifyIntent/createSession 函数体内有 LLM API 调用（非正则/parser）；层2运行时：至少1次真 LLM 调用确认 ANALYZING/IN_SESSION 走 LLM 路径 | 静态分析 + telemetry trace |

---

## 二、20+1 刁钻 case

### 正向流程（5条）

| # | Case | 预期行为 | 验证点 |
|---|------|---------|--------|
| 1 | 空输入（用户直接回车） | 不崩溃，提示"请说点什么" | 健壮性 |
| 2 | "记一笔午饭25" | category=午饭, amount=25, time=今天 → complete → EXECUTING(INSERT) → "已记录：午饭 25元" | record 完整链路 |
| 3 | "这个月花了多少" | intent=query → query_records → "这个月共花了XX元" → LISTENING（不经EXECUTING） | query 工具调用链路 |
| 4 | "删除刚才那笔" | intent=delete → 展示记录 → WAITING_CONFIRM → confirm → EXECUTING(DELETE) → "已删除" | delete 确认链路 |
| 5 | "这个月和上个月哪个花得多" | intent=compare → query_total("this_month")+query_total("last_month") → 格式化对比结果 → LISTENING | compare 工具调用链路 |

### 异常流程（8条）

| # | Case | 预期行为 | 验证点 |
|---|------|---------|--------|
| 6 | "记一笔9999999"（金额超限） | record → DET值域复验拦截 → CLARIFYING → "金额不能超过999999" | 降级链检查2 |
| 7 | "记一笔明天3000"（时间未来） | record → DET值域复验拦截 → CLARIFYING → "时间不能是未来" | 降级链检查2 |
| 8 | 记账中途说"查一下总共" | IN_SESSION(record) → off-task → ANALYZING重新识别 → IN_SESSION(query) | off-task双重保障 |
| 9 | 记账中途说"算了" | IN_SESSION(record) → giveup → LISTENING | giveup路径 |
| 10 | 记账中途说"切断房间" | IN_SESSION(record) → switch口令 → S3物理隔离 → ANALYZING(新房间+roomStateIndex) | M1 switch+S3 |
| 11 | 任何状态说退出词 | 任意 → CLOSING → IDLE | M1 exit |
| 12 | 连续多轮正常记账 | 上下文拼接不漂，不重复追问已采集字段 | 上下文管理器 |
| 13 | "改金额"（修改已填字段） | record → 回到 amount 重问 | modification路径 |

### 越界流程（4条）

| # | Case | 预期行为 | 验证点 |
|---|------|---------|--------|
| 14 | "帮我订机票" | intent=other + inputNature=T → TEMP_TOPIC | 第二分类维度T |
| 15 | "今天天气真好" | intent=other + inputNature=S → SLACK_NODE | 第二分类维度S |
| 16 | "你能帮我炒股吗" | SLACK_NODE → 闲聊回复（不编造功能） | 根宪法第4条 |
| 17 | "记错了，改成午饭30" | record → modification → 回到 amount 重问 | 修改机制 |

### 冷启动特殊（2条）

| # | Case | 预期行为 | 验证点 |
|---|------|---------|--------|
| 18 | 冷启动期内 logprobs 低于阈值 | 标记 logprob_warning 附加到返回，不直接拦截 | 冷启动软拦截 |
| 19 | 冷启动期后 logprobs 低于阈值 | DET硬拦截 → CLARIFYING | logprobs硬拦截 |

### 恢复路径（2条）

| # | Case | 预期行为 | 验证点 |
|---|------|---------|--------|
| 20 | "上次那个记账"（有未过期checkpoint） | ANALYZING查sessionCheckpoint→询问"继续or去其他环节"→继续→注入stepSnapshots→IN_SESSION(record) | sessionCheckpoint恢复 |
| 21 | "继续上次的查询" | 同理，query也有恢复路径 | 全环节恢复 |

---

## 三、9+2 机制检查

### 核心 9 项

| # | 机制 | 检查方法 | 预期 |
|---|------|---------|------|
| 1 | 状态机流转 | 所有转移路径至少触发一次（从 simulate 日志提取） | 32条转移边覆盖率≥90% |
| 2 | 契约校验 | contract-in/out 格式正确，非法输入被拦截 | DET 复验拦截率合理（5-15%） |
| 3 | 执行体分派 | LLM/DET/人各司其职 | ANALYZING/IN_SESSION走LLM，EXECUTING走DET |
| 4 | 路由表 | intent→环节宪法映射正确 | 5个intent全部映射到对应IN_SESSION |
| 5 | 宪法约束 | 根宪法4条+架构机制M1-M4+环节宪法全部生效 | 无违反环节隔离的跨任务操作 |
| 6 | 降级链 | 四项检查+硬编码兜底至少触发一次 | 冷启动期logprobs确认软拦截 |
| 7 | 上下文拼接命中率 | 模型不频繁重复追问已采集字段 | 重复追问率<10% |
| 8 | taskType 锚点机制 | field_based执行字段级硬门控 | askingField=amount时保护amount轮次 |
| 9 | v5.7/v5.8 对话分段保留 | 产出物确认后摘要生成+原始对话移到conversationArchive | segmentType变化正确 |

### +2 扩展项

| # | 机制 | 检查方法 | 预期 |
|---|------|---------|------|
| +1 | OpenTelemetry 追踪 | 每次DET/LLM/HUMAN调用生成span | trace链完整，可提取监控指标 |
| +2 | L2→L3 语义一致性 | DET自动提取 `<!-- @rule -->` 标记，对比L3宪法 | 全部规则在L3中保留 |

---

## 四、20+1 case 运行结果预期

| 指标 | 预期值 | 说明 |
|------|--------|------|
| case通过率 | ≥95%（21条中≥20条通过） | 允许1条边界case失败 |
| 转移边覆盖率 | ≥90% | 32条边至少29条被触发 |
| logprobs拦截率 | 5-15% | 冷启动期软拦截，冷启动后硬拦截 |
| 偏离率 | <10% | off-task触发频率 |
| 上下文重复追问率 | <10% | 模型不需要重复追问已采集字段 |
| changeLevel分布 | major<3%/minor>97%/invalid<5% | field_based changeLevel=null，不适用 |

---

## 五、交付检查

| 检查项 | 通过标准 |
|--------|---------|
| contractStore SQLite+WAL 实现 | roomConversationLog 含 v5.7 字段 + conversationArchive FTS5 |
| 宪法冲突检测引擎 | json-rules-engine / Drools |
| L2→L3 语义一致性校验 | `<!-- @rule -->` 标记自动提取对比 |
| 冷启动+软拦截+PID反馈控制 | cold_start_observation_window内logprobs软拦截，非冷启动硬拦截 |
| OpenTelemetry 追踪 | 每次DET/LLM/HUMAN调用生成span |
| v5.7/v5.8 分段保留完整 | segmentType/summaryImportance/originalTurnRange + conversationArchive FTS5 |
