# 富贵小安 变更日志

> **本文件是富贵小安所有产出物变更的唯一记录**——细则文件中不写任何变更过程，所有变更统一记录在此。
> **格式纪律**：按版本倒序排列（最新在最前），按节点列出变更。

---

## v5.6（2026-07-18）

### 文件管理
- **新增**：富贵小安变更日志.md——所有产出物变更统一记录
- **移动**：旧版本产出物（L2-N1/N3/N4/N5/N6/N7-N10 v5.3）移到 旧版本/ 文件夹
- **纪律**：以后修改产出物另存新版本，不直接覆盖原文件

### N1 场景定义与边界划定
- **重做**：基于 v5.6 基线重新走 N1（N1节点说明 v7.0 + 环节宪法 v8.7）
- **新增**：引导性硬拒策略（guided_reject）——含 {{anchorText}} 占位符 + 兜底逻辑
- **新增**：完整 JSON Schema 传递契约（N1_BoundaryList_TransferContract）
- **新增**：N2 执行模式决策（A/B/C）
- **修改**：tunable 参数从 18 个改为 22 个（通用 16 + field_based 2 + topic_based 4）
- **修改**：metaInstructions 只含 wake/exit/cancel 三类（switch 是架构级口令，不在 N1 定义）
- **不变**：场景定义、doList、taskType、松弛节点等核心内容不变

### 基线版本
- 总纲：v5.6（04核心机制-v5.6.md + 12附录-v5.6.md）
- N1节点说明：v7.0
- N1环节宪法：v8.7
- N3节点说明：v6.0
- N3环节宪法：v1.8

### N3 状态枚举与执行体分派
- **重做**：基于 v5.6 基线重新走 N3（N3节点说明 v6.0 + 环节宪法 v1.8）
- **新增**：完整 JSON Schema 传递契约（N3_StateEnumeration_TransferContract）
- **新增**：dispatchCriteria 结构化字段（costLevel/canHardcode/hasObjectiveAnswer/taskType/topicEvolutionEnabled）
- **新增**：publicRuleCompliance 合规检查（rule2 + rule3）
- **新增**：intentCoverageCheck 完整覆盖检查（含 softLandingCheck）
- **修改**：EXECUTING 职责调整（v5.4——仅写入/删除，查询/对比在IN_SESSION内通过工具调用完成）
- **修改**：IN_SESSION 增加 tool calling 能力（v5.4）+ 工具分层发现（v5.5）
- **不变**：状态集（9个状态）、IN_SESSION子类型（5个）、执行体分派核心逻辑

### N4 状态转移图与路由表
- **重做**：基于 v5.6 基线重新走 N4（N4节点说明 v6.0 + 环节宪法 v1.6）
- **新增**：完整 JSON 转移边列表（28条边，含transitionId）
- **新增**：sessionCheckpoint 恢复路径全部4个业务环节（record/query/delete/compare）
- **修改**：query/compare 不经EXECUTING（v5.4——在IN_SESSION内通过工具调用完成）
- **修改**：降级链五项→四项（移除跨任务延伸检测）
- **修改**：M1口令含S3物理隔离+roomStateIndex
- **不变**：转移图核心结构、路由表两层模型选择（全null第二层）

### N5 调度器核心逻辑
- **重做**：基于 v5.6 基线重新走 N5（N5节点说明 v6.1 + 环节宪法 v2.0）
- **新增**：工具分层注入（v5.5——必用+清单+选用+search_tools meta-tool+Two-Phase Selection）
- **新增**：IN_SESSION function calling（v5.4——query/compare在IN_SESSION内通过工具调用完成）
- **新增**：off-task双重保障（宪法强制+ANALYZING重新识别验证，取消DET事后验证）
- **新增**：第二分类维度S/T/U路由（S→SLACK_NODE, T→TEMP_TOPIC, U→追问）
- **新增**：S3物理隔离四步+roomStateIndex注入（switch口令触发）
- **修改**：降级链五项→四项（移除跨任务延伸检测）
- **修改**：confidence→logprobs（API返回真实概率）
- **修改**：sessionCheckpoint恢复路径全4个业务环节
- **不变**：五层过滤核心结构、上下文管理器三接口、接口隔离原则

### N6 数据传递协议与守卫规则
- **重做**：基于 v5.6 基线重新走 N6（N6节点说明 v3.0 + 环节宪法 v1.5）
- **新增**：26条转移路径（含sessionCheckpoint恢复4条+M1口令2条+SLACK_NODE 2条）
- **新增**：contractStore 11张表（新增toolRegistry + projectRegistry/userLastProject）
- **修改**：降级链五项→四项（移除跨任务延伸检测）
- **修改**：query/compare不经EXECUTING（v5.4——在IN_SESSION内通过工具调用完成）
- **修改**：roomConversationLog保留策略改为v5.7对话分段保留机制（产出物确认节点分段+摘要分级保留）
- **修改**：M1四类口令+wake/exit/cancel/switch+S3物理隔离
- **新增**：N1 tunableDeclarations增加4个场景特有参数（conversation_segmentation_enabled/summary_retention_critical/high/normal）
- **不变**：数据传递协议核心逻辑、守卫规则优先级

---

## v5.8（2026-07-19）

### N7-N10 宪法体系
- **重做**：基于 v5.8 基线重新走 N7-N10
- **N9 环节宪法**：6份完整宪法（含 @section tools/@section tool_catalog v5.5工具分层）
- **N10 tunable**：23个适用参数（通用16+field_based2+v5.7/v5.8新增5个）
- **other-session taskType**：修正为 N/A（路由分发器，不进行字段采集）

### N11-N12 契约对齐与拆包
- **新增**：27项契约对齐检查全部通过
- **新增**：工具 contract-in/out schema（5必用+3选用+search_tools）
- **新增**：L3 拆包结构（13个文件）+ 过滤清单 + `<!-- @rule -->` 标记检查

### N13 骨架生成
- **新增**：14个骨架代码文件清单 + 8项架构决策落地 + 编程规则宪法7条校验

### N14 审骨架
- **新增**：审骨架8项 + 20+1刁钻case + 9+2机制检查

### N15 调参交付
- **新增**：交付物四件套清单 + 9个tunable参数保持默认 + N1-N16全流程状态

### L3 v5.8 生成包
- **新增**：18个JSON文件（boundary/states/transitions/routeTable/scheduler/dataProtocol/root-constitution/tunables/tools/interfaces/contractStore + 7份constitutions）
- **修复**：tools.json catalogTools 补充 query_records
- **修复**：other-session taskType 改为 N/A
- **新增**：analyzing-session.json（ANALYZING宪法）
- **强化**：interfaces.json 补充三层职责（匹配层/决策层/拼接层）

### 编程阶段
- **新增**：src-v5.8/tool-registry.js（5必用+4选用+search_tools+时间解析）
- **新增**：src-v5.8/state-machine.js（10状态+五层过滤+off-task双重保障+S3+v5.4 tool calling）

### 基线版本
- 总纲：v5.8（04核心机制-v5.8.md + 12附录-v5.8.md + 11受控调参-v5.8.md）
- 节点说明：N1 v7.0 / N3 v6.0 / N4 v6.0 / N5 v6.1 / N6 v3.0 / N7 v2.0 / N8 v2.0 / N9 v2.0 / N10 v1.2 / N11 v1.2 / N12 v1.4 / N13 v1.3

---

## v5.3（2026-07-17）—— 旧版本（已移到旧版本/）

### N1-N10 初版产出
- N1 场景定义与边界划定
- N2 跳过（all_intents_field_based）
- N3 状态枚举与执行体分派
- N4 状态转移图与路由表
- N5 调度器核心逻辑
- N6 数据传递协议与守卫规则
- N7-N10 宪法体系
- **问题**：基于 v5.3 基线，缺少引导性硬拒、JSON Schema传递契约、工具分层等 v5.5/v5.6 新增机制
