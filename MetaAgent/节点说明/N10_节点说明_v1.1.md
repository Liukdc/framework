# N10 节点说明（元智能体 v1.1）
**版本**：v1.1 | **基于母本**：N10 节点说明 v1.1 / MetaAgent L2 §6

## 一、定位
N10 声明 tunable 参数清单——通用9个+topic_based专用8个=17个运行时参数。每个参数含默认值/取值范围/用途/消费方。冷启动配置独立声明。

## 二、通用参数（9个）
min_match_score(0.3)/confidence_threshold(0.4)/turnHistory_limit(20)/pending_fields_ttl(24h)/cold_start_observation_window(50)/cold_start_emergency_threshold(0.5)/pid_kp(0.1)/boundary_coverage_threshold(0.85)/contract_inheritance_limit([0.1,0.5],默认0.3)

## 三、topic_based专用参数（8个）
topic_anchor_similarity_threshold(0.6)/topic_keyword_overlap_threshold(0.2)/topic_evolution_match_threshold(0.65)/topic_evolution_minor_compaction_threshold(5)/domain_rule_validation_threshold(0.7)/rule_mining_schedule("0 2 * * *")/rule_mining_batch_threshold(5)/rule_mining_batch_size(10)

## 四、冷启动配置
四参数冻结(confidence_threshold/observation_window/emergency_threshold/pid_kp)+软拦截。解冻后PID仅调节confidence_threshold。observation_window常量。emergency_threshold仅手动调参。pid_kp比例系数常量。

## 五、自检清单
- [ ] 通用9参数齐全 / topic_based 8参数齐全 / contract_inheritance_limit [0.1,0.5] / 冷启动机制完整 / PID仅调节confidence_threshold
