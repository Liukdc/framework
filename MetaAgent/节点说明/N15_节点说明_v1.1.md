# N15 节点说明（元智能体 v1.1）
**版本**：v1.1 | **基于母本**：N15 节点说明 v1.2

## 一、定位
N15 是收官节点——根据N14测试结果微调参数+确认四件套齐全一致+打包交付作为运行时基线。

## 二、调参信号（12类）
confidence拦截率>10%→降/#<1%→升/跨任务延伸拦截率高→不调参回溯N9/major占比<3%→先回溯N9再调整压缩阈值/minor压缩过高→降压缩阈值/上下文命中率低(topic_based)→降相似度阈值/跨会话匹配失败率高→降匹配阈值/候选规则确认率低→升验证阈值/队列积压→升提炼频率/event写入低→查代码不调参/冷启动异常→应急解冻

## 三、交付物四件套
L2人读母本/L3生成包/骨架代码/simulate报告。18项一致性检查（L2与L3语义一致/L3与骨架代码一致/tunable与N10一致/版本号一致/changeLevelReason四件套一致/contractStore SQLite+WAL/冷启动完整/宪法冲突检测引擎/OpenTelemetry追踪/...）

## 四、调参安全边界
取值范围不得超出N10/r单次幅度≤30%/PID仅调节confidence_threshold/每次调参后回归测试(case11/12/16/18+机制6/9/+1)

## 五、自检清单
- [ ] 12类信号全评估 / 调参后回归测试通过 / 四件套齐全版本一致 / changeLevelReason四件套统一(记录警告) / 18项全过
