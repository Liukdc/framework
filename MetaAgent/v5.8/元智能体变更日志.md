# 元智能体 变更日志

> 所有产出物变更统一记录在此。

---

## v5.8（2026-07-20）

### N13 骨架代码（2026-07-20）
- **13 文件 / 1874 行**（含 N14 修复），位于 `packages/metaagent/src-v5.8/`
- 核心模块：index.js(主入口) / scheduler.js(五层过滤+N2双角色+N13代码专项,340+行) / state-machine.js(8状态+17子类型) / context-manager.js(三层注入) / route-table.js(11路由) / deepseek-adapter.js(analyzing+inSession+N13) / tool-registry.js(v5.5工具分层) / contract-store.js(SQLite+WAL+FTS5+S3边界) / tunables.js(25参数+冲突检测)
- 辅助模块：constitutions/loader.js / outputs-manager.js / telemetry.js / l2-l3-validator.js
- CLI 模式：`node index.js "用户输入"`

### N14 审骨架（2026-07-20）
8项审查全通过，发现并修复 4 个 bug：
1. **N13 代码专项模型未被使用**：scheduler.js 对所有 intent 统一调 `callInSession`，N13 误用对话模型 → 加 `intent==='N13'` 分支调 `callCodeModel`
2. **l2-l3-validator 期望转移边数错误**：写死 22 条实际 20 条 → 修正为 20
3. **switch 口令 S3 未执行物理隔离**：只做状态转移无 KV Cache 释放无 roomStateIndex → 追加 `appendSegmentBoundary` + roomStateIndex 注入
4. **N2 双角色串行未实现**：`_n2Role1Output` 恒为 null → 实现 `_runN2DualRole`（角色一只注场景定义生成语料，角色二注角色一输出+N1边界逐条审查）

### N15 调参交付（2026-07-20）
- 25 参数全部通过校验（range/enum 硬限制 + 跨参数联动约束）
- 测试覆盖：默认值/边界值/越界回退/跨参数联动
- 代码中使用 14/25 参数，11 个为架构占位（L2-N10 预期行为）

### L3 生成包对照修复（2026-07-20）
- **漏洞2修复**：routeTable.json 补 `IN_SESSION→LISTENING(turnType=giveup)` 边
- **漏洞3修复**：routeTable.json 补 WAITING_CONFIRM 出口两条（decision=confirm→IN_SESSION / decision=reject→ANALYZING）
- **漏洞1修复**：states.json P0 补 `importance: "high"`
- **漏洞4修复**：新增 `outputs.json`（6 critical + 4 high + 6 normal，含写盘策略声明）
- routes 从 8→11 条，L3 文件从 9→10 个

## v5.8（2026-07-20）

### N1 场景定义与边界划定
- **重做**：基于 v5.8 基线重新走 N1
- **修正**：taskType 从全 field_based 改为 14个 topic_based + 2个 field_based（N11/N12）
- **修正**：N2 不再错误跳过（有 topic_based intent 必须执行）
- **新增**：topicEvolution 启用（14个 topic_based intent）
- **新增**：降级链五项→四项 / confidence→logprobs / 第二分类维度S/T/U / 引导性硬拒
- **不变**：场景定义、16个intent结构、松弛节点不启用

---

## v2.0（2026-07-11）—— 旧版本
- 基于态控架构 v4.7
- taskType 全 field_based（错误）
- 使用 confidence
- 降级链五项
